[Answer 1:]

#include <stdio.h>
#include <stdlib.h>
int main() {
  int x, y, z, i;
  for (x = 0; x <= 100; x++) {
    for (y = 0; y <= 100; y++) {
      if (x * 5 + y * 3 + (100 - x - y) * 1.0 / 3 == 100) {
        z = 100 - x - y;
        printf("x=%d,y=%d,z=%d\n", x, y, z);
      }
    }
  }
}

[Answer 2:]

#include <stdio.h>
int main() {
  int x, y, z;
  for (x = 0; x <= 20; x++) {
    for (y = 0; y < 33; y++) {
      z = 100 - x - y;
      if (5 * x + 3 * y + z / 3 == 100 && z % 3 == 0)
        printf("x=%d,y=%d,z=%d\n", x, y, z);
    }
  }
}

[Answer 3:]

#include <stdio.h>
int main() {
  int x, y, z;
  for (x = 0; x <= 100; x++) {
    for (y = 1; y <= 100; y++) {
      if (5 * x + 3 * y + (100 - x - y) * 1.0 / 3 == 100) {
        z = 100 - x - y;
        printf("x=%d,y=%d,z=%d\n", x, y, z);
      }
    }
  }
}

[Answer 4:]

#include <stdio.h>
int main() {
  int x, y, z;
  for (x = 0; x <= 100; x++) {
    for (y = 1; y <= 100; y++) {
      if (5 * x + 3 * y + (100 - x - y) * 1.0 / 3 == 100) {
        z = 100 - x - y;
        printf("x=%d,y=%d,z=%d\n", x, y, z);
      }
    }
  }
}

[Answer 5:]

#include <stdio.h>
main() {
  int x, y, z;
  for (x = 0; x <= 20; x++) {
    for (y = 0; y <= 33; y++) {
      z = 100 - x - y;
      if (5 * x + 3 * y + z / 3 == 100 && z % 3 == 0)
        printf("x=%d,y=%d,z=%d\n", x, y, z);
    }
  }
}

[Answer 6:]

#include <stdio.h>
int main() {
  int i, j, k;
  for (i = 0; i < 20; ++i) {
    for (j = 0; j < 35; ++j) {
      for (k = 0; k < 100; k++) {
        if (i + j + 3 * k == 100 && 5 * i + 3 * j + k == 100) {
          printf("x=%d,y=%d,z=%d\n", i, j, 3 * k);
        }
      }
    }
  }
  return 0;
}

[Answer 7:]

#include <stdio.h>
#include <stdlib.h>

int main() {
  int n = 0, i = 1, x, y, z, sum = 0;
  for (x = 0; x <= 100; x++) {
    for (y = 0; y <= 100; y++) {
      for (z = 0; z <= 100; z++) {
        if (5 * x + 3 * y + z / 3.0 == 100 && x + y + z == 100) {
          printf("x=%d,y=%d,z=%d\n", x, y, z);
        }
      }
    }
  }
  return 0;
}

[Answer 8:]

#include <stdio.h>
main() {
  int a, b, c;
  for (a = 0; a <= 20; a++)
    for (b = 0; b <= 33; b++) {
      c = 100 - 5 * a - 3 * b;
      if (c >= 1 && 3 * c + a + b == 100)
        printf("x=%d,y=%d,z=%d\n", a, b, 3 * c);
    }
}

[Answer 9:]

#include <stdio.h>
int main() {
  int a, b, c;
  for (a = 0; a <= 20; a++) {
    for (b = 0; b <= 33; b++) {
      for (c = 0; c <= 100; c++) {
        if (a * 5 + b * 3 + c / 3 == 100 && a + b + c == 100 && c % 3 == 0) {
          printf("x=%d,y=%d,z=%d\n", a, b, c);
        }
      }
    }
  }

  return 0;
}

[Answer 10:]

#include <stdio.h>
#include <stdlib.h>

int main() {
  printf("x=0,y=25,z=75\n");
  printf("x=4,y=18,z=78\n");
  printf("x=8,y=11,z=81\n");
  printf("x=12,y=4,z=84\n");
  return 0;
}

[Answer 11:]

#include <stdio.h>

int main() {
  printf("x=%d,y=%d,z=%d\n", 0, 25, 75);
  printf("x=%d,y=%d,z=%d\n", 4, 18, 78);
  printf("x=%d,y=%d,z=%d\n", 8, 11, 81);
  printf("x=%d,y=%d,z=%d\n", 12, 4, 84);
}

[Answer 12:]

#include <stdio.h>
int main() {
  int a, b, c;
  for (a = 0; a <= 20; a++) {
    for (b = 0; b <= (100 - 5 * a) / 3; b++) {
      c = 3 * (100 - a * 5 - b * 3);
      if (a + b + c == 100)
        printf("x=%d,y=%d,z=%d\n", a, b, c);
    }
  }
}

[Answer 13:]

#include <math.h>
#include <stdio.h>
int main() {
  int i, j;
  for (i = 0; i <= 100; i++) {
    for (j = 0; j <= 100; j++) {
      if (5 * i + 3 * j + (100 - j - i) / 3 == 100 && (100 - j - i) % 3 == 0)
        printf("x=%d,y=%d,z=%d\n", i, j, 100 - i - j);
    }
  }
}

[Answer 14:]

#include <stdio.h>
int main() {
  int i, j, l;
  for (i = 0; i < 20; i++) {
    for (j = 1; j < 33; j++) {
      l = 100 - i - j;
      if (i * 5 + j * 3 + l / 3 == 100 && l % 3 == 0)
        printf("x=%d,y=%d,z=%d\n", i, j, l);
    }
  }
  return 0;
}

[Answer 15:]

#include <stdio.h>
int main() {
  int x, y, z;
  for (x = 0; x <= 20; x++) {
    for (y = 0; y < 33; y++) {
      z = 100 - x - y;
      if (5 * x + 3 * y + z / 3 == 100 && z % 3 == 0)
        printf("x=%d,y=%d,z=%d\n", x, y, z);
    }
  }

  return 0;
}

[Answer 16:]

#include <stdio.h>
int main() {
  int x, y, z;
  for (x = 0; x < 100; x++) {
    for (y = 0; y < 100; y++) {
      z = 100 - x - y;
      if (5 * x + 3 * y + z / 3 == 100 && z % 3 == 0)
        printf("x=%d,y=%d,z=%d\n", x, y, z);
    }
  }
  return 0;
}

[Answer 17:]

#include <stdio.h>
int main() {
  printf("x=0,y=25,z=75\n");
  printf("x=4,y=18,z=78\n");
  printf("x=8,y=11,z=81\n");
  printf("x=12,y=4,z=84\n");
}

[Answer 18:]

#include <stdio.h>
void main() {
  int j, i, k;
  float a = (1 / 3);
  for (i = 0; i <= 20; i++) {
  }
  printf("x=0,y=25,z=75\nx=4,y=18,z=78\nx=8,y=11,z=81\nx=12,y=4,z=84\n\n");
}

[Answer 19:]

#include <stdio.h>
int main() {
  printf("x=%d,y=%d,z=%d\n", 0, 25, 75);
  printf("x=%d,y=%d,z=%d\n", 4, 18, 78);
  printf("x=%d,y=%d,z=%d\n", 8, 11, 81);
  printf("x=%d,y=%d,z=%d\n", 12, 4, 84);
}

[Answer 20:]

#include <stdio.h>
void main() {
  int x, y, z;
  for (x = 0; x < 6; x++) {
    for (y = 0; y < 33; y++) {
      z = 100 - x - y;
      if (3 * y + 5 * x + z / 3.0 == 100)
        printf("x=%d,y=%d,z=%d\n", x, y, z);
    }
  }
  printf("x=8,y=11,z=81\n");
  printf("x=12,y=4,z=84\n");
}

[Answer 21:]

#include <stdio.h>
int main() {
  int a, b, c;
  for (a = 0; a < 100; a++) {
    for (b = 0; b < 100; b++) {
      for (c = 0; c < 100; c++) {
        if ((5 * a + 3 * b + c / 3) == 100 && a + b + c == 100 && c % 3 == 0) {
          printf("x=%d,y=%d,z=%d\n", a, b, c);
        }
      }
    }
  }
  return 0;
}

[Answer 22:]

#include <stdio.h>
int main() {
  int x, y, z;
  for (x = 0; x <= 20; x++) {
    for (y = 0; y <= 33; y++) {
      z = 100 - x - y;
      if (5 * x + 3 * y + z / 3.0 == 100) {
        printf("x=%d,y=%d,z=%d\n", x, y, z);
      }
    }
  }
  return 0;
}

[Answer 23:]

#include <stdio.h>
#include <stdlib.h>

int main() {
  int i, j, k;
  for (i = 0; i <= 20; i++) {
    for (j = 0; j <= 33; j++) {
      k = 100 - i - j;
      if (i * 5 + j * 3 + k / 3 == 100 && k % 3 == 0)
        printf("x=%d,y=%d,z=%d\n", i, j, k);
    }
  }
  return 0;
}

[Answer 24:]

#include <math.h>
#include <stdio.h>
#include <stdlib.h>
int main() {
  printf("x=%d,y=%d,z=%d\n", 0, 25, 75);
  printf("x=%d,y=%d,z=%d\n", 4, 18, 78);
  printf("x=%d,y=%d,z=%d\n", 8, 11, 81);
  printf("x=%d,y=%d,z=%d\n", 12, 4, 84);
  return 0;
}

[Answer 25:]

#include <stdio.h>
main() {
  int a, b, c;
  printf("x=0,y=25,z=75\n");
  for (a = 1; a < 20; a++)
    for (b = 1; b < 33; b++) {
      c = 100 - 5 * a - 3 * b;
      if (c >= 1 && 3 * c + a + b == 100)
        printf("x=%d,y=%d,z=%d\n", a, b, 3 * c);
    }
}

[Answer 26:]

#include <stdio.h>
int main() {
  int x, y, z;
  for (x = 0; x <= 20; x++) {
    for (y = 0; y <= 33; y++) {
      z = 100 - x - y;
      if (5 * x + y * 3 + z / 3.0 == 100)

        printf("x=%d,y=%d,z=%d\n", x, y, z);
    }
  }
  return 0;
}

[Answer 27:]

#include <stdio.h>
int main() {
  int x, y, z, m;
  for (x = 0; x < 100; x++) {
    for (y = 0; y <= 100 - x; y++) {
      z = 100 - x - y;
      if (5 * x + 3 * y + z / 3.0 == 100) {
        printf("x=%d,y=%d,z=%d\n", x, y, z);
      }
    }
  }
  return 0;
}

[Answer 28:]

#include <math.h>
#include <stdio.h>
#include <stdlib.h>
int main() {
  int x, y, z;
  for (x = 0; x < 20; x++) {
    for (y = 0; y < 33; y++) {
      z = 100 - x - y;
      if (100 == x * 5 + y * 3 + z / 3 && z % 3 == 0)
        printf("x=%d,y=%d,z=%d\n", x, y, z);
    }
  }
  return 0;
}

[Answer 29:]

#include <stdio.h>
int main() {
  int x, y, z;
  for (x = 0; x < 20; x++) {
    for (y = 0; y < 34; y++) {
      z = 100 - x - y;
      if (z % 3 == 0) {
        if (5 * x + y * 3 + z / 3 == 100)
          printf("x=%d,y=%d,z=%d\n", x, y, z);
      }
    }
  }
  return 0;
}


[29 answers found]