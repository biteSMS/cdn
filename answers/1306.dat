[Answer 1:]

#include <stdio.h>

int main() {
  int peachNum = 1, i;
  for (i = 1; i < 10; i++) {
    peachNum = (peachNum + 1) * 2;
  }
  printf("桃子总数=%d\n", peachNum);
}

[Answer 2:]

#include <stdio.h>
int main() {
  int a = 1, b, t;
  scanf("%d", &t);
  for (t = 1; t < 10; t++) {
    b = (a + 1) * 2;
    a = b;
  }
  printf("桃子总数=%d\n", b);
  return 0;
}

[Answer 3:]

#include <stdio.h>
#include <stdlib.h>

int main() {
  int n = 0, i = 1, x = 1, y = 1, z, sum = 1;
  float a;
  for (x = 1; x <= 9; x++) {
    sum = (sum + 1) * 2;
  }
  printf("桃子总数=%d\n", sum);
  return 0;
}

[Answer 4:]

#include <stdio.h>
void main(void) {
  int i, sum = 1;
  for (i = 1; i < 10; i++) {
    sum = (sum + 1) * 2;
  }
  printf("桃子总数=%d\n", sum);
}


[4 answers found]