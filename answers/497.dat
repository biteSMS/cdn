[Answer 1:]

#include <stdio.h>
int getint(int min, int max);
int main() {
  int max = 100, min = 3;
  int n;
  n = getint(min, max);
  printf("你输入的整数为：%d\n", n);
}
int getint(int min, int max) {
  int n;
  do {
    printf("请输入[%d..%d]间的一个整数：\n", min, max);
    scanf("%d", &n);
  } while (n > max || n < min);
  return n;
}

[Answer 2:]

#include <stdio.h>
int getint(int min, int max);
int main(void) {
  printf("你输入的整数为：%d\n", getint(3, 100));
  return 0;
}
int getint(int min, int max) {
  int result;
  printf("请输入[%d..%d]间的一个整数：\n", min, max);
  scanf("%d", &result);
  while (result > max || result < min) {
    printf("请输入[%d..%d]间的一个整数：\n", min, max);
    scanf("%d", &result);
  }
  return result;
}


[2 answers found]