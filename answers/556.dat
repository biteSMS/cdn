[Answer 1:]

#include <math.h>
#include <stdio.h>
int count = 0;
/**/
void main() {
  int user = 0, computer = 0, total = 23;
  printf("这里是23根火柴游戏！！\n");
  printf("注意：最大移动火柴数目为三根\n");
  do {
    printf("请输入移动的火柴数目：\n");
    scanf("%d", &user);
    while (user > 3 || user <= 0) {
      printf("对不起！您输入的了不合适的数目，请点击任意键重新输入！\n");
      printf("您输入移动火柴数目：\n");
      computer = 2;
      total -= computer;
      printf("计算机移动的火柴数目为：%d\n", computer);
      printf("计算机移动后剩下的火柴数目为：%d\n", total);
      printf("请输入移动的火柴数目：\n");
      scanf("%d", &user);
    }
    total -= user;
    printf("您移动的火柴数目为：%d\n", user);
    printf("您移动后剩下的火柴数目为：%d\n", total);
    if (total > 0) {
      computer = ((total - 1) % 4) > 1 ? ((total - 1) % 4) : 1;
      total -= computer;
      printf("计算机移动的火柴数目为：%d\n", computer);
      printf("计算机移动后剩下的火柴数目为：%d\n", total);
    } else {
      computer = 1;
      total -= computer;
    }
  } while (total > 0);
  if (total < 0)
    printf("对不起！您输了！\n");
  else
    printf("恭喜您！您赢了！\n");
}


[1 answers found]