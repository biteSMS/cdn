[Answer 1:]

#include <stdio.h>
int main() {
  int n, a, s = 0;
  printf("\nInput an integer here please:");
  scanf(" %d", &n);
  printf("\n%d*%d*%d=", n, n, n);
  for (a = n * (n - 1) + 1; s < n * n * n - a; a += 2) {
    s += a;
    printf("%d+", a);
  }
  printf("%d", a);
  return 0;
}


[1 answers found]