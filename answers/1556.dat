[Answer 1:]


#include <stdio.h>
int Fun(int m);
int main() {
  int a;
  int b;
  printf("Please input a number:\n");
  scanf("%d", &b);
  while (b <= 1) {
    getchar();

    scanf("%d", &b);
  }
  a = Fun(b);
  if (a == 0) {
    printf("%d is a prime number\n", b);
  } else
    printf("%d is not a prime number\n", b);
  return 0;
}
int Fun(int m) {
  int i = 2, flag = 0;
  for (i = 2; i < m; i++) {
    if (m % i == 0) {
      flag = 1;
      break;
    }
  }
  if (flag == 0)
    return 0;
  else
    return 1;
}

[Answer 2:]

#include <stdio.h>
#include <stdlib.h>

int Fun(int m);
int main() {
  int n;
  printf("Please input a number:\n");
  do {
    scanf("%d", &n);
  } while (n <= 1);
  if (Fun(n) == 1) {
    printf("%d is a prime number\n", n);
  } else {
    printf("%d is not a prime number\n", n);
  }
  return 0;
}

int Fun(int m) {
  int i;
  for (i = 2; i < m; i++) {
    if (m % i == 0) {
      return 0;
    }
  }
  return 1;
}


[2 answers found]