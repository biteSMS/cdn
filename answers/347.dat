[Answer 1:]

#include <stdio.h>
int main() {
  int X, Y;
  printf("Enter integer X and Y:");
  scanf("%d%d", &X, &Y);
  if (X > Y)
    printf("X>Y\n");
  else if (X < Y)
    printf("X<Y\n");
  else
    printf("X=Y\n");
  return 0;
}

[Answer 2:]

#include <stdio.h>
int main() {
  int x, y;
  printf("Enter integer X and Y:");
  scanf("%d%d", &x, &y);
  if (x == y) {
    printf("X=Y\n");
  } else if (x > y) {
    printf("X>Y\n");
  } else {
    printf("X<Y\n");
  }
}

[Answer 3:]

#include <stdio.h>
void main() {
  int X, Y;
  printf("Enter integer X and Y:");
  scanf("%d %d", &X, &Y);
  if (X > Y)
    printf("X>Y\n");
  else if (X < Y)
    printf("X<Y\n");
  else if (X = Y)
    printf("X=Y\n");
}

[Answer 4:]

#include <stdio.h>
main() {
  int a, b;
  printf("Enter integer X and Y:");
  scanf("%d%d", &a, &b);
  if (a > b) {
    printf("X>Y\n");
  } else if (a < b) {
    printf("X<Y\n");
  } else
    printf("X=Y\n");
  return 0;
}

[Answer 5:]

#include <stdio.h>
main() {
  int x, y;
  printf("Enter integer X and Y:");
  scanf("%d%d", &x, &y);
  if (x > y) {
    printf("X>Y\n");
  } else if (x < y) {
    printf("X<Y\n");
  } else if (x = y) {
    printf("X=Y\n");
  }
  return 0;
}

[Answer 6:]

#include <stdio.h>
main() {
  int x, y;
  printf("Enter integer X and Y:");
  scanf("%d%d", &x, &y);
  if (x > y) {
    printf("X>Y\n", x, y);
  } else if (x < y) {
    printf("X<Y\n", x, y);
  } else {
    printf("X=Y\n", x, y);
  }
  return 0;
}

[Answer 7:]

#include <stdio.h>
int main() {
  int x, y;
  printf("Enter integer X and Y:");
  scanf("%d%d", &x, &y);
  if (x > y) {
    printf("X>Y\n");
  } else if (x < y) {
    printf("X<Y\n");
  } else {
    printf("X=Y\n");
  }
  return 0;
}

[Answer 8:]

#include <stdio.h>
int main() {
  int x, y;
  printf("Enter integer X and Y:");
  scanf("%d%d", &x, &y);
  if (x > y) {
    printf("X>Y\n");
  } else if (x < y) {
    printf("X<Y\n");
  } else {
    printf("X=Y\n");
  }
  return 0;
}

[Answer 9:]

#include <stdio.h>

int main() {
  int X, Y;
  printf("Enter integer X and Y:");
  scanf("%d%d", &X, &Y);
  if (X > Y)
    printf("X>Y\n");
  else if (X < Y)
    printf("X<Y\n");
  else
    printf("X=Y\n");
}

学号 02017211712 姓名 柳俊风
    得分 1336 实验得分 158 金币数 151 代码行数 3472 答题时间 96098

[Answer 10:]

#include <stdio.h>
int main() {
  int x, y;
  printf("Enter integer X and Y:");
  scanf("%d%d", &x, &y);
  if (x > y) {
    printf("X>Y\n");
  }
  if (x < y) {
    printf("X<Y\n");
  }
  if (x == y) {
    printf("X=Y\n");
  }
}

[Answer 11:]

#include <stdio.h>
int main() {
  int x, y;
  printf("Enter integer X and Y:");
  scanf("%d%d", &x, &y);
  if (x > y) {
    printf("X>Y\n");
  }
  if (x < y) {
    printf("X<Y\n");
  }
  if (x == y) {
    printf("X=Y\n");
  }
}

[Answer 12:]

#include <stdio.h>
int main() {
  int x, y;

  printf("Enter integer X and Y:");
  scanf("%d%d", &x, &y);
  if (x > y)
    printf("X>Y\n");
  else if (x < y)
    printf("X<Y\n");
  else
    printf("X=Y\n");
}

[Answer 13:]

#include <stdio.h>
int main() {
  int x, y;

  printf("Enter integer X and Y:");
  scanf("%d%d", &x, &y);

  if (x > y) {
    printf("X>Y\n");
  } else if (x == y) {
    printf("X=Y\n");
  } else {
    printf("X<Y\n");
  }
}

[Answer 14:]

#include <stdio.h>
main() {
  int x, y;
  printf("Enter integer X and Y:");
  scanf("%d%d", &x, &y);
  if (x > y)
    printf("X>Y\n");
  if (x < y)
    printf("X<Y\n");
  if (x == y)
    printf("X=Y\n");
}

[Answer 15:]

#include <stdio.h>
int main() {
  int x, y;
  printf("Enter integer X and Y:");
  scanf("%d%d", &x, &y);
  if (x > y)
    printf("X>Y\n");
  else if (x < y)
    printf("X<Y\n");
  else
    printf("X=Y\n");
  return 0;
}

[Answer 16:]

#include <stdio.h>
int main() {
  int X, Y;
  printf("Enter integer X and Y:");
  scanf("%d%d", &X, &Y);
  if (X > Y)
    printf("X>Y\n");
  else if (X < Y)
    printf("X<Y\n");
  else
    printf("X=Y\n");
}

[Answer 17:]

#include <stdio.h>
#include <stdlib.h>

int main() {
  int x, y;
  printf("Enter integer X and Y:");
  scanf("%d%d", &x, &y);
  if (x > y) {
    printf("X>Y\n");
  }
  if (x == y) {
    printf("X=Y\n");
  }
  if (x < y) {
    printf("X<Y\n");
  }

  return 0;
}

[Answer 18:]

#include <stdio.h>
#include <stdlib.h>

int main() {
  int x, y;
  printf("Enter integer X and Y:");
  scanf("%d%d", &x, &y);
  if (x > y) {
    printf("X>Y\n");
  }
  if (x == y) {
    printf("X=Y\n");
  }
  if (x < y) {
    printf("X<Y\n");
  }

  return 0;
}

[Answer 19:]

#include <stdio.h>
int main() {
  printf("Enter integer X and Y:");
  int x, y;
  scanf("%d%d", &x, &y);
  if (x > y) {
    printf("X>Y\n");
  } else if (x < y) {
    printf("X<Y\n");
  } else {
    printf("X=Y\n");
  }
  return 0;
}

[Answer 20:]

#include <stdio.h>
#include <stdlib.h>
int main() {
  int a, b, c, d, e;
  printf("Enter integer X and Y:");
  scanf("%d%d", &a, &b);
  if (a > b) {
    printf("X>Y\n");
  } else if (a < b) {
    printf("X<Y\n");
  } else {
    printf("X=Y\n");
  }
  return 0;
}

[Answer 21:]

#include <stdio.h>

int main() {
  int x, y;

  printf("Enter integer X and Y:");
  scanf("%d%d", &x, &y);
  if (x > y) {
    printf("X>Y\n");
  } else if (x < y) {
    printf("X<Y\n");
  } else {
    printf("X=Y\n");
  }

  return 0;
}

[Answer 22:]

#include <math.h>
#include <stdio.h>
main() {
  int a, b;

  printf("Enter integer X and Y:");
  scanf("%d%d", &a, &b);
  if (a > b)
    printf("X>Y\n");
  if (a < b)
    printf("X<Y\n");
  if (a == b)
    printf("X=Y\n");
}

[Answer 23:]

#include <stdio.h>
int main() {
  int a, b;
  printf("Enter integer X and Y:");
  scanf("%d%d", &a, &b);
  if (a == b) {
    printf("X=Y\n", a, b);
  } else if (a < b) {
    printf("X<Y\n", a, b);
  } else if (a > b) {
    printf("X>Y\n", a, b);
  }
}

[Answer 24:]

#include <stdio.h>
int main() {
  int x, y;

  printf("Enter integer X and Y:");
  scanf("%d%d", &x, &y);

  if (x < y)
    printf("X<Y\n");
  else if (x > y)
    printf("X>Y\n");
  else
    printf("X=Y\n");
}

[Answer 25:]

#include <stdio.h>
int main() {
  int X, Y;
  printf("Enter integer X and Y:");
  scanf("%d%d", &X, &Y);
  if (X > Y) {
    printf("X>Y\n");
  } else if (X < Y) {
    printf("X<Y\n");
  } else if (X == Y) {
    printf("X=Y\n");
  }
}

[Answer 26:]

#include <stdio.h>
int main() {
  printf("Enter integer X and Y:");
  int x, y;
  scanf("%d%d", &x, &y);
  if (x < y) {
    printf("X<Y\n");
  } else if (x > y) {
    printf("X>Y\n");
  } else {
    printf("X=Y\n");
  }

  return 0;
}

[Answer 27:]

#include <math.h>
#include <stdio.h>

int main() {
  int x = 0, y = 0;
  printf("Enter integer X and Y:");
  scanf("%d%d", &x, &y);
  if (x - y == 0)
    printf("X=Y\n");
  else if (x - y > 0)
    printf("X>Y\n");
  else
    printf("X<Y\n");
  return 0;
}

[Answer 28:]

#include <stdio.h>
int main() {
  printf("Enter integer X and Y:");
  int a = 0, b = 0;
  scanf("%d%d", &a, &b);
  printf("X%cY", a > b ? '>' : (a < b ? '<' : '='));
  return 0;
}

[Answer 29:]

#include <math.h>
#include <stdio.h>
#include <stdlib.h>

int main() {
  int a, b, c;
  printf("Enter integer X and Y:");
  scanf("%d%d", &a, &b);
  if (a > b)
    printf("X>Y\n");
  if (a < b)
    printf("X<Y\n");
  if (a == b)
    printf("X=Y\n");
}

[Answer 30:]

#include <math.h>
#include <stdio.h>
#include <string.h>

int main() {
  int a, b;
  printf("Enter integer X and Y:");
  scanf("%d%d", &a, &b);
  if (a > b)
    printf("X>Y\n");
  else if (a < b)
    printf("X<Y\n");
  else
    printf("X=Y\n");
}

[Answer 31:]

#include <stdio.h>
int main() {
  int a, b;
  printf("Enter integer X and Y:");
  scanf("%d%d", &a, &b);
  if (a > b)
    printf("X>Y\n");
  else if (a < b)
    printf("X<Y\n");
  else
    printf("X=Y\n");
}

[Answer 32:]

#include <stdio.h>
int main() {
  int x, y;
  printf("Enter integer X and Y:");
  scanf("%d%d", &x, &y);
  if (x < y)
    printf("X<Y\n");
  else if (x > y)
    printf("X>Y\n");
  else
    printf("X=Y\n");
}

[Answer 33:]

#include <stdio.h>
int main() {
  int n, m;
  printf("Enter integer X and Y:");
  scanf("%d%d", &n, &m);
  if (n > m)
    printf("X>Y\n");
  else if (n < m)
    printf("X<Y\n");
  else
    printf("X=Y\n");
}

[Answer 34:]

#include <stdio.h>
int main() {
  int x, y;
  printf("Enter integer X and Y:");
  scanf("%d%d", &x, &y);
  if (x > y)
    printf("X>Y\n");
  else if (x < y)
    printf("X<Y\n");
  else
    printf("X=Y\n");
  return 0;
}

[Answer 35:]

#include <stdio.h>
int main() {
  int x, y;
  printf("Enter integer X and Y:");
  scanf("%d%d", &x, &y);
  if (x > y)
    printf("X>Y\n");
  else if (x < y)
    printf("X<Y\n");
  else
    printf("X=Y\n");
  return 0;
}


[35 answers found]