[Answer 1:]

#include <stdio.h>
#include <stdlib.h>
int main() {
  int i, m1 = 0, m2 = 0, m3 = 0, m4 = 0, m5 = 0, k = 0;
  char s[80];
  printf("请输入一行字符：");
  gets(s);
  for (i = 0; s[i] != '\0'; i++) {
    if (s[i] == ' ' || s[i + 1] == '\0') {
      if (k == 1)
        m1++;
      if (k >= 2 && k <= 5)
        m2++;
      if (k >= 6 && k <= 10)
        m3++;
      if (k >= 11 && k <= 20)
        m4++;
      if (k >= 21)
        m5++;
      k = 0;
    }
    k++;
  }
  printf("长度为1的单词数：%d\n", m1);
  printf("长度在2到5的单词数：%d\n", m2);
  printf("长度在6到10的单词数：%d\n", m3);
  printf("长度在11到20的单词数：%d\n", m4);
  printf("长度大于等于21的单词数：%d\n", m5);
  return 0;
}


[1 answers found]