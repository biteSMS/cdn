[Answer 1:]

#include "stdio.h"
#include "string.h"
main() {
  int n[10];
  int i, a = 0, b = 0, c = 0;
  printf("请输入10个整数：");
  for (i = 0; i < 10; i++) {
    scanf("%d", &n[i]);
  }
  for (i = 0; i < 10; i++) {
    if (n[i] > 0)
      a++;
    else if (n[i] == 0)
      b++;
    else
      c++;
  }
  printf("\n正数个数: %d\n负数个数: %d\n零的个数: %d\n", a, c, b);
}

[Answer 2:]

#include <stdio.h>
#include <stdlib.h>
int fun(int m);

int main() {
  int num[10], z = 0, f = 0, l = 0, i;
  printf("请输入10个整数：");
  for (i = 0; i < 10; i++) {
    scanf("%d", &num[i]);
  }
  for (i = 0; i < 10; i++) {
    if (num[i] > 0)
      z++;
    else if (num[i] == 0)
      l++;
    else if (num[i] < 0)
      f++;
  }
  printf("\n正数个数: %d\n负数个数: %d\n零的个数: %d\n", z, f, l);
  return 0;
}

[Answer 3:]

#include <stdio.h>
#include <stdlib.h>

int main() {
  int a, b, c, d, e, f, g, h, i, j, x1 = 0, x2 = 0, x3 = 0;
  printf("请输入10个整数：");
  scanf("%d %d %d %d %d %d %d %d %d %d", &a, &b, &c, &d, &e, &f, &g, &h, &i,
        &j);
  while (a > 0) {
    x1++;
    break;
  }
  while (a == 0) {
    x2++;
    break;
  }
  while (a < 0) {
    x3++;
    break;
  }
  while (b > 0) {
    x1++;
    break;
  }
  while (b == 0) {
    x2++;
    break;
  }
  while (b < 0) {
    x3++;
    break;
  }
  while (c > 0) {
    x1++;
    break;
  }
  while (c == 0) {
    x2++;
    break;
  }
  while (c < 0) {
    x3++;
    break;
  }
  while (d > 0) {
    x1++;
    break;
  }
  while (d == 0) {
    x2++;
    break;
  }
  while (d < 0) {
    x3++;
    break;
  }
  while (e > 0) {
    x1++;
    break;
  }
  while (e == 0) {
    x2++;
    break;
  }
  while (e < 0) {
    x3++;
    break;
  }
  while (f > 0) {
    x1++;
    break;
  }
  while (f == 0) {
    x2++;
    break;
  }
  while (f < 0) {
    x3++;
    break;
  }
  while (g > 0) {
    x1++;
    break;
  }
  while (g == 0) {
    x2++;
    break;
  }
  while (g < 0) {
    x3++;
    break;
  }
  while (h > 0) {
    x1++;
    break;
  }
  while (h == 0) {
    x2++;
    break;
  }
  while (h < 0) {
    x3++;
    break;
  }
  while (i > 0) {
    x1++;
    break;
  }
  while (i == 0) {
    x2++;
    break;
  }
  while (i < 0) {
    x3++;
    break;
  }
  while (j > 0) {
    x1++;
    break;
  }
  while (j == 0) {
    x2++;
    break;
  }
  while (j < 0) {
    x3++;
    break;
  }
  printf("\n正数个数: %d\n负数个数: %d\n零的个数: %d\n", x1, x3, x2);
  return 0;
}

[Answer 4:]

#include <stdio.h>
#define N 10
main() {
  int i, m = 0, n = 0, k = 0, a[N];
  printf("请输入10个整数：");
  for (i = 0; i < N; i++) {
    scanf("%d", &a[i]);
  }
  for (i = 0; i < N; i++) {
    if (a[i] > 0)
      m++;
    else if (a[i] < 0)
      n++;
    else
      k++;
  }
  printf("\n正数个数: %d\n负数个数: %d\n零的个数: %d\n", m, n, k);
}


[4 answers found]