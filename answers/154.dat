[Answer 1:]


#include <stdio.h>
main() {
  int x, y, z;
  for (x = 0; x <= 20; x++) {
    for (y = 0; y <= 33; y++) {
      z = 100 - x - y;
      if ((5 * x + 3 * y + z / 3) == 100 && z % 3 == 0) {
        printf("x=%d, y=%d, z=%d\n", x, y, z);
      }
    }
  }
}

[Answer 2:]


#include <stdio.h>
main() {
  int x, y, z;
  for (x = 0; x <= 20; x++) {
    for (y = 0; y <= 33; y++) {
      z = 100 - x - y;
      if ((5 * x + 3 * y + z / 3) == 100 && (z % 3 == 0)) {
        printf("x=%d, y=%d, z=%d\n", x, y, z);
      }
    }
  }
}

[Answer 3:]

#include <stdio.h>
main() {
  int x, y, z;
  for (x = 0; x <= 20; x++) {
    for (y = 0; y <= 33; y++) {
      z = 100 - x - y;
      if (5 * x + 3 * y + z / 3 == 100 && z >= 0 && z % 3 == 0) {
        printf("x=%d, y=%d, z=%d\n", x, y, z);
      }
    }
  }
}

[Answer 4:]


#include <stdio.h>
main() {
  int x, y, z;
  for (x = 0; x <= 20; x++) {
    for (y = 0; y <= 33; y++) {
      z = 100 - x - y;
      if (5 * x + 3 * y + z / 3 == 100 && z % 3 == 0) {
        printf("x=%d, y=%d, z=%d\n", x, y, z);
      }
    }
  }
  return 0;
}

[Answer 5:]

#include <stdio.h>
main() {
  float x, y, z;
  for (x = 0; x <= 20; x++) {
    for (y = 0; y <= 33; y++) {
      z = 100 - x - y;
      if (5 * x + 3 * y + z / 3 == 100) {
        printf("x=%d, y=%d, z=%d\n", (int)x, (int)y, (int)z);
      }
    }
  }
}

[Answer 6:]

#include <stdio.h>
main() {
  int x, y, z;
  for (x = 0; x <= 20; x++) {
    for (y = 0; y <= 33; y++) {
      z = 100 - x - y;
      if (5 * x + 3 * y + (float)z / 3 == 100) {
        printf("x=%d, y=%d, z=%d\n", x, y, z);
      }
    }
  }
}

[Answer 7:]

#include <stdio.h>
main() {
  int x, y, z;
  for (x = 0; x <= 20; x++) {
    for (y = 0; y <= 33; y++) {
      z = 100 - x - y;
      if (5 * x + 3 * y + z / 3 == 100 && z % 3 == 0) {
        printf("x=%d, y=%d, z=%d\n", x, y, z);
      }
    }
  }
}

[Answer 8:]

#include <stdio.h>
main() {
  int x, y, z;
  for (x = 0; x <= 20; x++) {
    for (y = 0; y <= 33; y++) {
      z = 100 - x - y;
      if (5 * x + 3 * y + z / 3 == 100 && z % 3 == 0) {
        printf("x=%d, y=%d, z=%d\n", x, y, z);
      }
    }
  }
}

[Answer 9:]

#include <stdio.h>
int main() {
  int x, y, z;
  for (x = 0; x <= 20; x++) {
    for (y = 0; y <= 33; y++) {
      z = 100 - x - y;

      if (5 * x + 3 * y + z / 3 == 100 && z % 3 == 0) {
        printf("x=%d, y=%d, z=%d\n", x, y, z);
      }
    }
  }
  return 0;
}

[Answer 10:]

#include <stdio.h>
int main() {
  int x, y, z;
  for (x = 0; x <= 20; x++) {
    for (y = 0; y <= 33; y++) {
      z = 100 - x - y;
      if (z % 3 == 0 && 5 * x + 3 * y + z / 3 == 100) {
        printf("x=%d, y=%d, z=%d\n", x, y, z);
      }
    }
  }
  return 0;
}

[Answer 11:]

#include <stdio.h>
main() {
  int x, y, z;
  for (x = 0; x <= 20; x++) {
    for (y = 0; y <= 33; y++) {
      z = 100 - x - y;
      if (5 * x + 3 * y + (float)z / 3 == 100) {
        printf("x=%d, y=%d, z=%d\n", x, y, z);
      }
    }
  }
}

[Answer 12:]

#include <stdio.h>
main() {
  int x, y, z;
  for (x = 0; x <= 20; x++) {
    for (y = 0; y <= 33; y++) {
      z = 100 - x - y;
      if (5 * x + 3 * y + z / 3 == 100 && z % 3 == 0) {
        printf("x=%d, y=%d, z=%d\n", x, y, z);
      }
    }
  }
}

[Answer 13:]

#include <stdio.h>
main() {
  int x, y, z;
  for (x = 0; x <= 20; x++) {
    for (y = 0; y <= 33; y++) {
      z = 100 - x - y;
      if (5 * x + 3 * y + (float)z / 3 == 100) {
        printf("x=%d, y=%d, z=%d\n", x, y, z);
      }
    }
  }
}

[Answer 14:]

#include <stdio.h>
main() {
  int x, y, z;
  for (x = 0; x <= 20; x++) {
    for (y = 0; y <= 33; y++) {
      z = 100 - x - y;
      if (15 * x + 9 * y + z == 300) {
        printf("x=%d, y=%d, z=%d\n", x, y, z);
      }
    }
  }
}


[14 answers found]