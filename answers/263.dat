[Answer 1:]

#include <stdio.h>
#include <stdlib.h>

int main() {
  int m;
  char a[13][20] = {"January",   "February", "March",    "April",
                    "May",       "June",     "July",     "August",
                    "September", "October",  "November", "December"};

  printf("Input month number:");
  scanf("%d", &m);

  if (0 < m && m < 13)
    printf("month %d is %s\n", m, *(a + m - 1));
  else
    printf("Illegal month");

  return 0;
}

[Answer 2:]

#include <stdio.h>
#include <stdlib.h>

int main() {
  int m;
  char a[12][20] = {"January",   "February", "March",    "April",
                    "May",       "June",     "July",     "August",
                    "September", "October",  "November", "December"};

  printf("Input month number:");
  scanf("%d", &m);

  if (0 < m && m < 13)
    printf("month %d is %s\n", m, *(a + m - 1));
  if (m < 1 || m > 12)
    printf("Illegal month");

  return 0;
}

[Answer 3:]

#include <stdio.h>

int main() {
  int i, mon;
  char *month[] = {"Illegal month", "January",   "February", "March",
                   "April",         "May",       "June",     "July",
                   "August",        "September", "October",  "November",
                   "December"};
  printf("Input month number:");
  scanf("%d", &mon);
  if (mon >= 1 && mon <= 12)
    printf("month %d is %s\n", mon, *(month + mon));
  else
    printf("%s", *month);
  return 0;
}

[Answer 4:]

#include <stdio.h>
int main() {
  char a[13][20] = {"Illegal month", "January",   "February", "March",
                    "April",         "May",       "June",     "July",
                    "August",        "September", "October",  "November",
                    "December"};
  int s;
  printf("Input month number:");
  scanf("%d", &s);
  if (s < 1 || s > 12) {
    printf("Illegal month");
  } else {
    printf("month %d is %s\n", s, a[s]);
  }
}

[Answer 5:]

#include <stdio.h>
int main() {
  printf("Input month number:");
  char *p[12] = {"January",   "February", "March",    "April",
                 "May",       "June",     "July",     "August",
                 "September", "October",  "November", "December"};
  int i;
  scanf("%d", &i);
  if (0 <= i && i <= 12)
    printf("month %d is %s\n", i, p[i - 1]);
  else
    printf("Illegal month");
}

[Answer 6:]

#include <stdio.h>
int main() {
  printf("Input month number:");
  char *p[12] = {"January",   "February", "March",    "April",
                 "May",       "June",     "July",     "August",
                 "September", "October",  "November", "December"};
  int i;
  scanf("%d", &i);
  if (0 <= i && i <= 12)
    printf("month %d is %s\n", i, p[i - 1]);
  else
    printf("Illegal month");
  return 0;
}

[Answer 7:]

#include <stdio.h>
int main() {
  int n, i, flag = 0;
  char month[12][10] = {"January",   "February", "March",    "April",
                        "May",       "June",     "July",     " August",
                        "September", "October",  "November", "December"};
  char *p = month;
  printf("Input month number:");
  scanf("%d", &n);
  for (i = 0; i <= 12; i++) {
    if (n == i) {
      printf("month %d is %s\n", n, month[i - 1]);
      flag = 1;
      break;
    }
  }
  if (!flag)
    printf("Illegal month");
  return 0;
}

[Answer 8:]

#include <stdio.h>
main() {
  int m;
  const char *a[13] = {"Illegal month", "January",   "February", "March",
                       "April",         "May",       "June",     "July",
                       "August",        "September", "October",  "November",
                       "December"};
  printf("Input month number:");
  scanf("%d", &m);
  if (1 <= m && m <= 12)
    printf("month %d is %s", m, a[m]);
  else
    printf("Illegal month");
}

[Answer 9:]

#include <stdio.h>
main() {
  int m;
  const char *a[13] = {"Illegal month", "January",   "February", "March",
                       "April",         "May",       "June",     "July",
                       "August",        "September", "October",  "November",
                       "December"};
  printf("Input month number:");
  scanf("%d", &m);
  if (1 <= m && m <= 12)
    printf("month %d is %s", m, a[m]);
  else
    printf("Illegal month");
}

[Answer 10:]

#include <stdio.h>
#define N 10
int main() {
  char month[12][N] = {"January",   "February", "March",    "April",
                       "May",       "June",     "July",     "August",
                       "September", "October",  "November", "December"};
  char *pstr[12];
  int n, i;
  printf("Input month number:");
  scanf("%d", &n);
  for (i = 0; i < 12; i++) {
    pstr[i] = month[i];
  }
  if (n >= 1 && n <= 12) {
    printf("month %d is %s\n", n, pstr[n - 1]);
  } else
    printf("Illegal month");
  return 0;
}

[Answer 11:]

#include <stdio.h>
#define N 10
int main() {
  char month[12][N] = {"January",   "February", "March",    "April",
                       "May",       "June",     "July",     "August",
                       "September", "October",  "November", "December"};
  char *pstr[12];
  int i, n;
  for (i = 0; i < 12; i++) {
    pstr[i] = month[i];
  }
  printf("Input month number:");
  scanf("%d", &n);
  if (n >= 1 && n <= 12)
    printf("month %d is %s\n", n, pstr[n - 1]);
  else
    printf("Illegal month");
  return 0;
}

[Answer 12:]

#include <stdio.h>
#include <stdlib.h>

int main() {
  char b[12][20] = {"January",   "February", "March",    "April",
                    "May",       "June",     "July",     "August",
                    "September", "October",  "November", "December"};
  int a;
  printf("Input month number:");
  scanf("%d", &a);
  if (a >= 13 || a <= 0) {
    printf("Illegal month");
  } else {
    printf("month %d is %s\n", a, b[a - 1]);
  }

  return 0;
}

[Answer 13:]

#include <stdio.h>
int main() {
  int n;
  char monthName[][20] = {"Illegal month", "January",   "February", "March",
                          "April",         "May",       "June",     "July",
                          "August",        "September", "October",  "November",
                          "December"};

  printf("Input month number:");
  scanf("%d", &n);
  if ((n <= 12) && (n >= 1)) {
    printf("month %d is %s\n", n, monthName[n]);

  } else {
    printf("%s\n", monthName[0]);
  }
}

[Answer 14:]

#include <stdio.h>
int main() {
  int n;
  char *a[] = {"January",   "February", "March",    "April",
               "May",       "June",     "July",     "August",
               "September", "October",  "November", "December"};
  printf("Input month number:");
  scanf("%d", &n);
  if (n > 0 && n <= 12) {
    printf("month %d is %s\n", n, a[n - 1]);
  } else {
    printf("Illegal month");
  }
  return 0;
}

[Answer 15:]

#include <stdio.h>
#include <stdlib.h> //!!!!!!!!!!!
#include <string.h>

int main() {
  char *a[13] = {"error",   "January",  "February", "March",   "April",
                 "May",     "June",     "July",     " August", "September",
                 "October", "November", "December"};
  int i, n;
  printf("Input month number:");
  scanf("%d", &n);
  if (n == 13) {
    printf("Illegal month");
  } else {
    printf("month %d is %s\n", n, a[n]);
  }
}

[Answer 16:]

#include <stdio.h>
int main() {
  int m;
  char *s[1300] = {"",        "January",  "February", "March",  "April",
                   "May",     "June",     "July",     "August", "September",
                   "October", "November", "December"};
  printf("Input month number:");
  scanf("%d", &m);

  if (m > 0 && m < 13)
    printf("month %d is %s\n", m, s[m]);
  else
    printf("Illegal month\n");
  return 0;
}

[Answer 17:]

#include <stdio.h>
int main() {
  char *p[20] = {"Illegal month", "January",  "February", "March",  "April",
                 "May",           "June",     "July",     "August", "September",
                 "October",       "November", "December"};
  int n;
  printf("Input month number:");
  scanf("%d", &n);
  if (n >= 1 && n <= 12) {
    printf("month %d is %s", n, p[n]);
  } else {
    puts(p[0]);
  }
  return 0;
}

[Answer 18:]

#include <stdio.h>
int main() {
  int n;
  char month[][20] = {"January",      "February", "March",    "April",
                      "May",          "June",     "July",     "August",
                      "September",    "October",  "November", "December",
                      "Illegal month"};
  char ch;
  printf("Input month number:");
  scanf("%d", &n);
  if (n < 1 || n > 12) {
    printf("Illegal month");
  } else if (n == 1) {
    printf("month %d is %s\n", n, month[n - 1]);
  } else if (n == 2) {
    printf("month %d is %s\n", n, month[n - 1]);
  } else if (n == 3) {
    printf("month %d is %s\n", n, month[n - 1]);
  } else if (n == 4) {
    printf("month %d is %s\n", n, month[n - 1]);
  } else if (n == 5) {
    printf("month %d is %s\n", n, month[n - 1]);
  } else if (n == 6) {
    printf("month %d is %s\n", n, month[n - 1]);
  } else if (n == 7) {
    printf("month %d is %s\n", n, month[n - 1]);
  } else if (n == 8) {
    printf("month %d is %s\n", n, month[n - 1]);
  } else if (n == 9) {
    printf("month %d is %s\n", n, month[n - 1]);
  } else if (n == 10) {
    printf("month %d is %s\n", n, month[n - 1]);
  } else if (n == 11) {
    printf("month %d is %s\n", n, month[n - 1]);
  } else if (n == 12) {
    printf("month %d is %s\n", n, month[n - 1]);
  }
  return 0;
}

[Answer 19:]

#include <stdio.h>
#include <stdlib.h>
#define N 12
int main() {
  int n, i;
  char name[N][N] = {"January",   "February", "March",    "April",
                     "May",       "June",     "July",     "August",
                     "September", "October",  "November", "December"};
  char *pmonth[N];
  for (i = 0; i < N; i++) {
    pmonth[i] = name[i];
  }
  printf("Input month number:");
  scanf("%d", &n);
  if (n > 12 || n <= 0)
    printf("Illegal month");
  else
    printf("month %d is %s\n", n, pmonth[n - 1]);
  return 0;
}

[Answer 20:]

#include <stdio.h>
int main() {
  char *str[13] = {"Illegal month", "January",   "February", "March",
                   "April",         "May",       "June",     "July",
                   "August",        "September", "October",  "November",
                   "December"};
  int n;
  printf("Input month number:");
  scanf("%d", &n);
  if (n >= 1 && n <= 12)
    printf("month %d is %s\n", n, str[n]);
  else
    printf("%s", str[0]);
  return 0;
}

[Answer 21:]

#include <stdio.h>
int main() {
  char *Month[12] = {"January",   "February", "March",    "April",
                     "May",       "June",     "July",     "August",
                     "September", "October",  "November", "December"};
  int month;
  printf("Input month number:");
  scanf("%d", &month);
  if (month < 0 || month > 12) {
    printf("Illegal month");
  } else {
    printf("month %d is %s\n", month, Month[month - 1]);
  }
}

[Answer 22:]

#include <stdio.h>
main() {
  int a;
  char s[][12] = {"January",   "February", "March",    "April",
                  "May",       "June",     "July",     "August",
                  "September", "October",  "November", "December"};
  printf("Input month number:");
  scanf("%d", &a);
  if (a < 1 || a > 12) {
    printf("Illegal month\n");
  } else {
    printf("month %d is %s\n", a, s[a - 1]);
  }
  return 0;
}

[Answer 23:]

#include <stdio.h>

int main() {
  char mouth[][10] = {"January",   "February", "March",    "April",
                      "May",       "June",     "July",     "August",
                      "September", "October",  "November", "December"};
  int n;
  printf("Input month number:");
  scanf("%d", &n);
  if (n < 13) {
    printf("month %d is %s\n", n, mouth[n - 1]);
  } else {
    printf("Illegal month");
  }
}

[Answer 24:]

#include <stdio.h>
int main() {
  char a[13][20] = {"Illegal month", "January",   "February", "March",
                    "April",         "May",       "June",     "July",
                    "August",        "September", "October",  "November",
                    "December"};
  int m;
  printf("Input month number:");
  scanf("%d", &m);
  if (m >= 1 && m <= 12) {
    printf("month %d is %s\n", m, a[m]);
  } else {
    printf("Illegal month");
  }
}

[Answer 25:]

#include <stdio.h>
int main() {
  int n;
  char *monthName[] = {"Illegal month", "January",   "February", "March",
                       "April",         "May",       "June",     "July",
                       "August",        "September", "October",  "November",
                       "December"};
  printf("Input month number:");
  scanf("%d", &n);
  if (n >= 1 && n <= 12) {
    printf("month %d is %s\n", n, monthName[n]);
  } else {
    printf("%s\n", monthName[0]);
  }
  return 0;
}

[Answer 26:]

#include <stdio.h>
main() {
  int n;
  char *a[] = {"January",   "February", "March",    "April",
               "May",       "June",     "July",     "August",
               "September", "October",  "November", "December"};
  printf("Input month number:");
  scanf("%d", &n);
  if (n > 12 || n < 1)
    printf("Illegal month");
  else
    printf("month %d is %s\n", n, a[n - 1]);
}

[Answer 27:]

#include <stdio.h>
main() {
  char *month[] = {"Illegal month", "January",   "February", "March",
                   "April",         "May",       "June",     "July",
                   "August",        "September", "October",  "November",
                   "December"};
  int n;
  printf("Input month number:");
  scanf("%d", &n);
  if (n < 1 || n > 12) {
    printf("%s\n", month[0]);
  } else
    printf("month %d is %s\n", n, month[n]);
}


[27 answers found]