[Answer 1:]

#include <stdio.h>
int main() {
  int x, y, z;
  for (x = 0; x <= 100; x++) {
    for (y = 1; y <= 100; y++) {
      if (5 * x + 3 * y + (100 - x - y) * 1.0 / 3 == 100) {
        z = 100 - x - y;
        printf("x=%d,y=%d,z=%d\n", x, y, z);
      }
    }
  }
}

[Answer 2:]

#include <stdio.h>
int main() {
  int x, y, z;
  for (x = 0; x <= 100; x++) {
    for (y = 1; y <= 100; y++) {
      if (5 * x + 3 * y + (100 - x - y) * 1.0 / 3 == 100) {
        z = 100 - x - y;
        printf("x=%d,y=%d,z=%d\n", x, y, z);
      }
    }
  }
}

[Answer 3:]

#include <stdio.h>
#include <stdlib.h>

int main() {
  int n = 0, i = 1, x, y, z, sum = 0;
  for (x = 0; x <= 100; x++) {
    for (y = 0; y <= 100; y++) {
      for (z = 0; z <= 100; z++) {
        if (5 * x + 3 * y + z / 3.0 == 100 && x + y + z == 100) {
          printf("x=%d,y=%d,z=%d\n", x, y, z);
        }
      }
    }
  }

  return 0;
}

[Answer 4:]

#include <stdio.h>
void main(void) {
  int i, j;
  for (i = 0; i <= 100; i++) {
    for (j = 0; j <= 100 - i; j++) {
      if (i * 5 + j * 3 + (100 - i - j) / 3 == 100 && (100 - i - j) % 3 == 0) {
        printf("x=%d,y=%d,z=%d\n", i, j, 100 - i - j);
      }
    }
  }
}

[Answer 5:]

#include <stdio.h>

int main() {
  int x, y, z;

  for (x = 0; x <= 20; x++) {
    for (y = 0; y <= 33; y++) {
      z = 100 - x - y;
      if (5 * x + 3 * y + z / 3 == 100 && z % 3 == 0) {
        printf("x=%d,y=%d,z=%d\n", x, y, z);
        break;
      }
    }
  }
}

[Answer 6:]

#include <stdio.h>
int main() {
  int a, b, c;
  for (a = 0; a < 20; a++) {
    for (b = 0; b < 33; b++) {
      for (c = 0; c < 100; c++) {
        if (a + b + c == 100 && a * 5 + b * 3 + c / 3 == 100 && c % 3 == 0) {
          printf("x=%d,y=%d,z=%d\n", a, b, c);
        }
      }
    }
  }
}

[Answer 7:]

#include <stdio.h>
#include <stdlib.h>
int main() {
  printf("x=0,y=25,z=75\n");
  printf("x=4,y=18,z=78\n");
  printf("x=8,y=11,z=81\n");
  printf("x=12,y=4,z=84\n");
  return 0;
}

[Answer 8:]

#include <math.h>
#include <stdio.h>
#include <stdio.h>
#include <string.h>
int main() {
  printf("x=%d,y=%d,z=%d\n", 0, 25, 75);
  printf("x=%d,y=%d,z=%d\n", 4, 18, 78);
  printf("x=%d,y=%d,z=%d\n", 8, 11, 81);
  printf("x=%d,y=%d,z=%d\n", 12, 4, 84);
}

[Answer 9:]

#include <stdio.h>
int main() {
  int a, b, c;
  for (a = 0; a <= 20; a++) {
    for (b = 0; b <= (100 - 5 * a) / 3; b++) {
      c = 3 * (100 - 5 * a - 3 * b);
      if (100 == a + b + c)
        printf("x=%d,y=%d,z=%d\n", a, b, c);
    }
  }
}

[Answer 10:]

#include <math.h>
#include <stdio.h>
int main() {
  int i, j;
  for (i = 0; i <= 100; i++) {
    for (j = 0; j <= 100; j++) {
      if (5 * i + 3 * j + (100 - j - i) / 3 == 100 && (100 - j - i) % 3 == 0)
        printf("x=%d,y=%d,z=%d\n", i, j, 100 - i - j);
    }
  }
}

[Answer 11:]

#include <stdio.h>
int main() {
  int x, y, z;
  for (x = 0; x <= 20; x++) {
    for (y = 0; y <= 33; y++) {
      z = 100 - x - y;
      if (5 * x + 3 * y + (float)z / 3 == 100)
        printf("x=%d,y=%d,z=%d\n", x, y, z);
    }
  }
  return 0;
}

[Answer 12:]

#include <stdio.h>
int main() {
  int x, y, z;
  for (x = 0; x <= 20; x++) {
    for (y = 0; y <= 33; y++) {
      z = 100 - 5 * x - 3 * y;
      if (3 * z + x + y == 100)
        printf("x=%d,y=%d,z=%d\n", x, y, 3 * z);
    }
  }
  return 0;
}

[Answer 13:]

#include <stdio.h>
int main(void) {
  int x, y, z;
  for (x = 0; x <= 20; x++) {
    for (y = 0; y <= 33; y++) {
      z = 100 - x - y;
      if (300 == 15 * x + 9 * y + z)
        printf("x=%d,y=%d,z=%d\n", x, y, z);
    }
  }
  return 0;
}

[Answer 14:]

#include <stdio.h>
int main() {
  int x, y, z;
  for (x = 0; x <= 20; x++) {
    for (y = 0; y < 33; y++) {
      z = 100 - x - y;
      if (5 * x + 3 * y + z / 3 == 100 && z % 3 == 0)
        printf("x=%d,y=%d,z=%d\n", x, y, z);
    }
  }
  return 0;
}

[Answer 15:]

#include <stdio.h>
int main() {
  int x, y, z;
  for (x = 0; x < 100; x++) {
    for (y = 0; y < 100; y++) {
      z = 100 - x - y;
      if (5 * x + 3 * y + z / 3 == 100 && z % 3 == 0)
        printf("x=%d,y=%d,z=%d\n", x, y, z);
    }
  }
  return 0;
}

[Answer 16:]

#include <stdio.h>
int main() {

  printf("x=0,y=25,z=75\n");
  printf("x=4,y=18,z=78\n");
  printf("x=8,y=11,z=81\n");
  printf("x=12,y=4,z=84\n");
}


[16 answers found]