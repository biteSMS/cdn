[Answer 1:]

#include <stdio.h>
int main() {
  int sum = 0, count = 0, m, n, o;
  while (m != 0) {
    printf("Input a number:");
    scanf("%d", &m);
    if (m > 0) {
      sum += m;
      count++;
    }
  }
  printf("sum = %d, count = %d\n", sum, count);
}

[Answer 2:]

#include <math.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
int main() {
  int n, sum = 0, count = 0;
  do {
    printf("Input a number:");
    scanf("%d", &n);
    if (n > 0) {
      sum += n;
      count++;
    }
  } while (n != 0);
  printf("sum = %d, count = %d\n", sum, count);
  return 0;
}

[Answer 3:]

#include <stdio.h>
main() {
  int a = 1, sum = 0, b = -1;
  while (a != 0) {
    printf("Input a number:");
    scanf("%d", &a);
    if (a >= 0) {
      sum += a;
      b += 1;
    }
  }
  printf("sum = %d, count = %d\n", sum, b);
}

[Answer 4:]

#include <stdio.h>
int main() {
  int n;
  int sum = 0, cou = 0;
  printf("Input a number:");
  scanf("%d", &n);
  while (n != 0) {
    if (n == 0)
      break;
    if (n > 0) {
      sum += n;
      cou++;
    }
    printf("Input a number:");
    scanf("%d", &n);
  }
  printf("sum = %d, count = %d\n", sum, cou);
}

[Answer 5:]

#include <stdio.h>
int main() {
  int n, sum = 0, count = 0;
  printf("Input a number:");
  scanf("%d", &n);
  if (n < 0) {
  } else {
    sum += n;
  }
  n = 1;
  while (n) {
    printf("Input a number:");
    scanf("%d", &n);
    if (n < 0) {
      continue;
    } else {
      sum += n;
      count++;
    }
  }
  printf("sum = %d, count = %d\n", sum, count);
  return 0;
}

[Answer 6:]

#include <stdio.h>
#include <stdlib.h>
int main() {
  int n, sum = 0, count = 0;
  printf("Input a number:");
  while (scanf("%d", &n), n != 0) {
    printf("Input a number:");
    if (n > 0) {
      sum += n;
      count++;
    }
  }
  printf("sum = %d, count = %d\n", sum, count);

  return 0;
}


[6 answers found]