[Answer 1:]

#include <stdio.h>
int main() {
  int year, month, day, sum = 0, s, i, t, x;
  int Day[2][13] = {{0, 31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31},
                    {0, 31, 29, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31}};
  x = scanf("%4d-%2d-%2d", &year, &month, &day);
  if (year < 1990 || !x)
    printf("Invalid input.");
  else {
    s = (year - 1990) / 4;
    sum = (year - 1990) * 365 + s;
    if ((year % 400 == 0) || ((year % 4 == 0) && (year % 100) != 0)) {
      for (i = 0; i < month; i++)
        sum += Day[1][i];
    } else {
      for (i = 0; i < month; i++)
        sum += Day[0][i];
    }
    sum += day;
    t = sum % 5;
    if (t >= 1 && t <= 3)
      printf("He is working.");
    else
      printf("He is having a rest.");
  }
}


[1 answers found]