[Answer 1:]


#include <stdio.h>
int main() {
  int x, y;
  printf("Input x:");
  scanf("%d", &x);
  if (x >= -5 && x <= 5) {
    y = -x;
  } else if (x == 10) {
    y = 100;
  } else {
    y = -x;
  }
  printf("f(%d)=%d", x, y);
}

[Answer 2:]

#include <stdio.h>
int main() {
  int x, y;
  printf("Input x:");
  scanf("%d", &x);
  if (x >= -5 && x <= 5) {
    y = -x;
  } else if (x == 10) {
    y = 100;
  } else {
    y = -x;
  }
  printf("f(%d)=%d", x, y);
}

[Answer 3:]

#include <stdio.h>
int main() {
  int x, y;
  printf("Input x:");
  scanf("%d", &x);
  if (x >= -5 && x <= 5) {
    y = -x;
  } else if (x == 10) {
    y = 100;
  } else {
    y = -x;
  }
  printf("f(%d)=%d", x, y);
}

[Answer 4:]

#include <stdio.h>
int main() {
  int x, y;
  printf("Input x:");
  scanf("%d", &x);
  if (-5 <= x && x <= 5) {
    y = -x;
  } else if (x == 10) {
    y = 100;
  } else {
    y = -x;
  }
  printf("f(%d)=%d", x, y);
}

[Answer 5:]

#include <stdio.h>
int main() {
  int x, y;
  printf("Input x:");
  scanf("%d", &x);
  if (x >= -5 && x <= 5) {
    y = -x;
  } else if (x == 10) {
    y = 100;
  } else {
    y = -x;
  }
  printf("f(%d)=%d", x, y);
}

[Answer 6:]

#include <stdio.h>
int main() {
  int x, y;
  printf("Input x:");
  scanf("%d", &x);
  if (-5 <= x && x <= 5) {
    y = -x;
  } else if (x == 10) {
    y = 100;
  } else {
    y = -x;
  }
  printf("f(%d)=%d", x, y);
}

[Answer 7:]

#include <stdio.h>
int main() {
  int x, y;
  printf("Input x:");
  scanf("%d", &x);
  if (x >= -5 && x <= 5) {
    y = -x;
  } else if (x == 10) {
    y = 100;
  } else {
    y = -x;
  }
  printf("f(%d)=%d", x, y);
  return 0;
}

[Answer 8:]

#include <stdio.h>
int main() {
  int x, y;
  printf("Input x:");
  scanf("%d", &x);
  if (-5 <= x && x <= 5) {
    y = -x;
  } else if (x == 10) {
    y = 100;
  } else {
    y = -x;
  }
  printf("f(%d)=%d", x, y);
}

[Answer 9:]

#include <stdio.h>
int main() {
  int x, y;
  printf("Input x:");
  scanf("%d", &x);
  if (x >= -5 && x <= 5) {
    y = -x;
  } else if (x == 10) {
    y = 100;
  } else {
    y = -x;
  }
  printf("f(%d)=%d", x, y);
}

[Answer 10:]

#include <stdio.h>
main() {
  int x, y;
  printf("Input x:");
  scanf("%d", &x);
  if (x >= -5 && x <= 5) {
    y = -x;
  } else if (x == 10) {
    y = 100;
  } else {
    y = -x;
  }
  printf("f(%d)=%d", x, y);
}

[Answer 11:]

#include <stdio.h>
int main() {
  int x, y;
  printf("Input x:");
  scanf("%d", &x);
  if (-5 <= x && x <= 5) {
    y = -x;
  } else if (x == 10) {
    y = 100;
  } else {
    y = -x;
  }
  printf("f(%d)=%d", x, y);
}


[11 answers found]