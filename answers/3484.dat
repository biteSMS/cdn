[Answer 1:]

//给定一个row行col列的整数数组array，要求从array[0][0]元素开始，按从右上到左下的对角线顺序遍历整个数组。
//输入的第一行上有两个整数，依次为row和col。（注：输入的row和col保证0 < row <
//100, 0 < col < 100）
//	余下有row行，每行包含col个整数，构成一个二维整数数组。
//	例如：3 4 1 2 4 7 3 5 8 10 6 9 11 12
//	表示为3行4列的数组
//	数组格式为： 1 2 4  7
//	3 5 8  10
//	6 9 11 12
//	最后按遍历顺序输出每个整数。每个整数占一行。

#include <stdio.h>
#include <stdlib.h>

int cols = 4;
int rows = 3;

int main() {
  printf("请分别输入行号和列号:");

  scanf("%d%d", &rows, &cols);

  int **matrix = (int **)malloc(rows * sizeof(int *));

  int i = 0, j = 0;

  for (i = 0; i < rows; i++)
    matrix[i] = (int *)malloc(cols * sizeof(int));

  printf("从第一行开始依次从左到右输入数组值:");
  for (i = 0; i < rows; i++) {
    for (j = 0; j < cols; j++) {
      scanf("%d", &matrix[i][j]);
    }
  }

  int times = 0;
  j = i = 0;
  while (times < rows * cols) {
    int temp = j;
    for (i = 0; i < rows; i++, j--) {
      if ((0 <= j && j < cols) && (0 <= i && i < rows)) {
        printf("%d\n", matrix[i][j]);
        times++;
      }
    }
    j = temp + 1;
  }

  for (i = 0; i < rows; i++)
    free(matrix[i]);

  free(matrix);
  return 0;
}


[1 answers found]