[Answer 1:]

#include <stdio.h>
int main() {
  int a, num;
  num = 0;
  printf("Please enter the number:\n");
  scanf("%d", &a);
  printf("%d: ", a);
  if (a < 0)
    a = -a;
  while (a) {
    a /= 10;
    num++;
  }
  printf("%d bits\n", num);
}

[Answer 2:]

#include <stdio.h>

int main() {
  int i, n1, n, count = 0;
  printf("Please enter the number:\n");
  scanf("%d", &n);
  n1 = n;
  do {
    count++;
    n1 /= 10;
  } while (n1);
  printf("%d: %d bits\n", n, count);
}

[Answer 3:]

#include <stdio.h>
int main() {
  int n, m, i = 0;
  printf("Please enter the number:\n");
  scanf("%d", &n);
  m = n;
  while (m) {
    m /= 10;
    i++;
  }
  printf("%d: %d bits\n", n, i);

  return 0;
}


[3 answers found]