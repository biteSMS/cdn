[Answer 1:]

#include <stdio.h>
main() {
  char str[3][20];
  int a = 0, b = 0, c = 0;
  int other = 0;
  int i, j;
  for (i = 0; i < 3; i++) {
    for (j = 0; (str[i][j] = getchar()) != '\n'; j++) {
      if (str[i][j] >= 'A' && str[i][j] <= 'Z')
        a++;
      else if (str[i][j] >= 'a' && str[i][j] <= 'z')
        a++;
      else if (str[i][j] >= '0' && str[i][j] <= '9')
        b++;
      else
        other++;
    }
  }
  printf("英文字母的个数为%d\n数字字符的个数为%d\n其它字符的个数%d\n", a, b,
         other);
}

学号 2017210283 姓名 杨正乾
    得分 845 实验得分 24.2 金币数 9 代码行数 1721 答题时间 53435

[Answer 2:]

#include <stdio.h>
#include <string.h>
main() {
  char a[20], b[20], c[20];
  int e = 0, s = 0, q = 0, i;
  gets(a);
  gets(b);
  gets(c);
  for (i = 0; i < strlen(a); i++) {
    if (a[i] >= '0' && a[i] <= '9')
      s++;
    else if (a[i] >= 'a' && a[i] <= 'z' || a[i] >= 'A' && a[i] <= 'Z')
      e++;
    else
      q++;
  }
  for (i = 0; i < strlen(b); i++) {
    if (b[i] >= '0' && b[i] <= '9')
      s++;
    else if (b[i] >= 'a' && b[i] <= 'z' || b[i] >= 'A' && b[i] <= 'Z')
      e++;
    else
      q++;
  }
  for (i = 0; i < strlen(c); i++) {
    if (c[i] >= '0' && c[i] <= '9')
      s++;
    else if (c[i] >= 'a' && c[i] <= 'z' || c[i] >= 'A' && c[i] <= 'Z')
      e++;
    else
      q++;
  }
  printf("英文字母的个数为%d\n数字字符的个数为%d\n其它字符的个数%d\n", e, s, q);
}

[Answer 3:]

#include <stdio.h>
int main() {
  int i, j, word, dig, oth;
  char text[3][20];
  word = dig = oth = 0;
  for (i = 0; i < 3; i++) {
    gets(text[i]);
    for (j = 0; j < 20 && text[i][j] != '\0'; j++) {
      if (text[i][j] >= 'A' && text[i][j] <= 'Z')
        word++;
      else if (text[i][j] >= 'a' && text[i][j] <= 'z')
        word++;
      else if (text[i][j] >= '0' && text[i][j] <= '9')
        dig++;
      else
        oth++;
    }
  }
  printf("英文字母的个数为%d\n", word);
  printf("数字字符的个数为%d\n", dig);
  printf("其它字符的个数%d\n", oth);
  return 0;
}


[3 answers found]