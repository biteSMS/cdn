[Answer 1:]

//程序：计算整数中数字的位数。
//**输入格式要求："%d" 提示信息："Enter a nonnegative integer:"
//**输出格式要求："The number has %d digit(s).\n"
//程序运行示例如下：
// Enter a nonnegative integer : 60
// The number has 2 digit(s).

#include <stdio.h>
#include <stdlib.h>

int func(int x) {
  int i = 1;
  while (x / 10) {
    i++;
    x /= 10;
  }

  return i;
}

int main() {
  printf("Enter a nonnegative integer:");
  int x = 0;
  scanf("%d", &x);
  printf("The number has %d digit(s).\n", func(x));
  return 0;
}

[Answer 2:]

#include <stdio.h>
int main() {
  int a, i, j = 0, m;
  printf("Enter a nonnegative integer:");
  scanf("%d", &a);
  for (i = 10; m != 0; i *= 10) {
    m = a / i;
    j++;
  }
  printf("The number has %d digit(s).\n", j);
  return 0;
}

[Answer 3:]

#include <stdio.h>
int main() {
  int n, i;
  printf("Enter a nonnegative integer:");
  scanf("%d", &n);
  for (i = 1; n >= 10; i++) {
    n = n / 10;
    ;
  }
  printf("The number has %d digit(s).\n", i);
  return 0;
}

[Answer 4:]

#include <stdio.h>
#include <stdlib.h>

int main() {
  int number;
  int i = 0, r = 1;
  printf("Enter a nonnegative integer:");
  scanf("%d", &number);
  if (number == 0)
    i++;
  while (number) {
    i++;
    number /= 10;
  }
  printf("The number has %d digit(s).\n", i);
  return 0;
}

[Answer 5:]

#include <math.h>
#include <stdio.h>
int main() {
  int n, c = 1;
  printf("Enter a nonnegative integer:");
  scanf("%d", &n);
  do {
    n = n / 10;
    if (n != 0)
      c++;
  } while (n != 0);
  printf("The number has %d digit(s).\n", c);
}

[Answer 6:]

#include <stdio.h>
int main() {
  int a, i;
  printf("Enter a nonnegative integer:");
  scanf("%d", &a);
  for (i = 0; a > 9; i++) {
    a = a / 10;
  }
  printf("The number has %d digit(s).\n", i + 1);
}

[Answer 7:]

#include <stdio.h>
main() {
  int n, m = 0;
  printf("Enter a nonnegative integer:");
  scanf("%d", &n);
  if (n == 0)
    printf("The number has %d digit(s).\n", 1);
  else {
    while (n > 0) {
      n = n / 10;
      m++;
    }
    printf("The number has %d digit(s).\n", m);
  }
}

[Answer 8:]

#include <stdio.h>
int main() {
  int x, i;
  printf("Enter a nonnegative integer:");
  scanf("%d", &x);
  if (x == 0) {
    printf("The number has 1 digit(s).\n", i);
    return 0;
  }
  for (i = 0; x != 0; i++) {
    x /= 10;
  }
  printf("The number has %d digit(s).\n", i);
}

[Answer 9:]

#include <stdio.h>
main() {
  int x;
  int j;
  int count = 0;
  printf("Enter a nonnegative integer:");
  scanf("%d", &x);
  j = x;
  do {

    j = j / 10;
    count++;
  } while (j != 0);
  printf("The number has %d digit(s).\n", count);
}

[Answer 10:]

#include <math.h>
#include <stdio.h>
int main() {
  int a, i = 0;
  printf("Enter a nonnegative integer:");
  scanf("%d", &a);
  a = fabs(a);
  if (a == 0) {
    printf("The number has %d digit(s).\n", 1);
    return 0;
  }

  while (a >= 1) {
    a /= 10;
    i++;
  }
  printf("The number has %d digit(s).\n", i);
  return 0;
}

[Answer 11:]

#include <stdio.h>
int main() {
  int i, j, n;
  printf("Enter a nonnegative integer:");
  scanf("%d", &n);
  for (i = 1; n / 10; i++) {
    n = n / 10;
  }
  printf("The number has %d digit(s).\n", i);
}

[Answer 12:]

#include <stdio.h>
main() {
  int n, count = 0;
  printf("Enter a nonnegative integer:");
  scanf("%d", &n);
  if (n > 10) {
    while (n != 0) {
      count++;
      n = n / 10;
    }
  } else {
    count = 1;
  }
  printf("The number has %d digit(s).\n", count);
}

[Answer 13:]

#include <stdio.h>
int main() {
  int n, i = 1;
  printf("Enter a nonnegative integer:");
  scanf("%d", &n);
  if (n == 0)
    printf("The number has 1 digit(s).");
  else {
    for (i; n != 0; i++) {
      n = n / 10;
    }
    i--;
    printf("The number has %d digit(s).\n", i);
  }

  return 0;
}

[Answer 14:]

#include <stdio.h>
int main() {
  int a, i;
  printf("Enter a nonnegative integer:");
  scanf("%d", &a);
  if (a >= 0 && a < 10)
    i = 1;
  else {
    for (i = 0; a != 0; i++)
      a = a / 10;
  }
  printf("The number has %d digit(s).\n", i);
  return 0;
}


[14 answers found]