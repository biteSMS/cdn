[Answer 1:]

#include <stdio.h>
int MyStrcmp(char s[], char t[]);
int main() {
  char str1[20], str2[20];
  printf("Input string:");
  gets(str1);
  printf("Input another string:");
  gets(str2);
  if (MyStrcmp(str1, str2) > 0) {
    printf("str1 大于 str2\n");
  } else if (MyStrcmp(str1, str2) < 0) {
    printf("str1 小于 str2\n");
  } else {
    printf("str1 等于 str2\n");
  }
}
int MyStrcmp(char s[], char t[]) {
  int i;
  for (i = 0; s[i] == t[i]; i++)
    ;
  {
    if (s[i] == '\0')
      return 0;
  }
  return (s[i] - t[i]);
}

[Answer 2:]

#include <stdio.h>
int MyStrcmp(char s[], char t[]);
int main() {
  char str1[20], str2[20];
  printf("Input string:");
  gets(str1);
  printf("Input another string:");
  gets(str2);
  if (MyStrcmp(str1, str2) > 0) {
    printf("str1 大于 str2\n");
  } else if (MyStrcmp(str1, str2) < 0) {
    printf("str1 小于 str2\n");
  } else {
    printf("str1 等于 str2\n");
  }
}
int MyStrcmp(char s[], char t[]) {
  int i;
  for (i = 0; s[i] == t[i]; i++)
    ;
  {
    if (s[i] == '\0')
      return 0;
  }
  return (s[i] - t[i]);
}

[Answer 3:]

#include <stdio.h>

int MyStrcmp(char s[], char t[]);
int main() {
  char str1[20];
  char str2[20];

  printf("Input string:");
  gets(str1);
  printf("Input another string:");
  gets(str2);

  if (MyStrcmp(str1, str2) > 0) {
    printf("str1 大于 str2\n");
  } else if (MyStrcmp(str1, str2) < 0) {
    printf("str1 小于 str2\n");
  } else {
    printf("str1 等于 str2\n");
  }

  return 0;
}

int MyStrcmp(char s[], char t[]) {
  int i;

  for (i = 0; s[i] == t[i]; i++) {
    if (s[i] == '\0')
      return 0;
  }
  return (s[i] - t[i]);
}

学号 02017211706 姓名 袁鸿逸
    得分 421 实验得分 10 金币数 45 代码行数 1213 答题时间 31140

[Answer 4:]

#include <stdio.h>
int MyStrcmp(char s[], char t[]);
main() {
  char str1[20], str2[20];
  printf("Input string:");
  gets(str1);
  printf("Input another string:");
  gets(str2);
  if (MyStrcmp(str1, str2) > 0) {
    printf("str1 大于 str2\n");
  } else if (MyStrcmp(str1, str2) < 0) {
    printf("str1 小于 str2\n");
  } else {
    printf("str1 等于 str2\n");
  }
}
int MyStrcmp(char s[], char t[]) {
  int i;
  for (i = 0; s[i] == t[i]; i++) {
    if (s[i] == '\0' && t[i] == '\0')
      return 0;
  }
  return (s[i] - t[i]);
}

[Answer 5:]

#include <stdio.h>
#include <string.h>

int MyStrcmp(char s[], char t[]) {
  int i, count;
  for (i = 0; s[i] == t[i] && (i < strlen(s) && i < strlen(t)); i++)
    ;
  count = s[i] - t[i];
  return count;
}

int main() {
  const int size = 80;
  char s[size], t[size];
  int count;
  printf("Input string:");
  gets(s);
  printf("Input another string:");
  gets(t);
  count = MyStrcmp(s, t);
  if (count > 0)
    printf("str1 大于 str2\n");
  else if (count < 0)
    printf("str1 小于 str2\n");
  else
    printf("str1 等于 str2\n");
  return 0;
}

[Answer 6:]

#include <stdio.h>
#include <string.h>

int MyStrcmp(char s[], char t[]) {
  int i, count;
  for (i = 0; s[i] == t[i] && (i < strlen(s) && i < strlen(t)); i++)
    ;
  count = s[i] - t[i];
  return count;
}

int main() {
  const int size = 80;
  char s[size], t[size];
  int count;
  printf("Input string:");
  gets(s);
  printf("Input another string:");
  gets(t);
  count = MyStrcmp(s, t);
  if (count > 0)
    printf("str1 大于 str2\n");
  else if (count < 0)
    printf("str1 小于 str2\n");
  else
    printf("str1 等于 str2\n");
  return 0;
}

[Answer 7:]

#include <stdio.h>
int MyStrcmp(char s[], char t[]);
main() {
  char str1[20], str2[20];
  printf("Input string:");
  gets(str1);
  printf("Input another string:");
  gets(str2);
  if (MyStrcmp(str1, str2) > 0) {
    printf("str1 大于 str2\n");
  } else if (MyStrcmp(str1, str2) < 0) {
    printf("str1 小于 str2\n");
  } else {
    printf("str1 等于 str2\n");
  }
}
int MyStrcmp(char s[], char t[]) {
  int i;
  for (i = 0; s[i] == t[i] && i < 20; i++)
    ;
  {
    if (s[i] == '\0' && t[i] == '\0')
      return 0;
  }
  return (s[i] - t[i]);
}

[Answer 8:]

#include <stdio.h>
int MyStrcmp(char s[], char t[]);
main() {
  char str1[20], str2[20];
  printf("Input string:");
  gets(str1);
  printf("Input another string:");
  gets(str2);
  if (MyStrcmp(str1, str2) > 0) {
    printf("str1 大于 str2\n");
  } else if (MyStrcmp(str1, str2) < 0) {
    printf("str1 小于 str2\n");
  } else {
    printf("str1 等于 str2\n");
  }
}
int MyStrcmp(char s[], char t[]) {
  int i;
  for (i = 0; s[i] == t[i]; i++) {
    if (s[i] = '\0')
      return 0;
  }
  return (s[i] - t[i]);
}

[Answer 9:]

#include <stdio.h>
int MyStrcmp(char s[], char t[]);
main() {
  char str1[20], str2[20];

  printf("Input string:");
  gets(str1);
  printf("Input another string:");
  gets(str2);

  if (MyStrcmp(str1, str2) > 0) {
    printf("str1 大于 str2\n");
  } else if (MyStrcmp(str1, str2) < 0) {
    printf("str1 小于 str2\n");
  } else {
    printf("str1 等于 str2\n");
  }
}

int MyStrcmp(char s[], char t[]) {
  int i;

  for (i = 0; s[i] == t[i]; i++) {
    if (s[i] = '\0')
      return 0;
  }
  return (s[i] - t[i]);
}

[Answer 10:]

#include <stdio.h>

int MyStrcmp(char s[], char t[]);
main() {
  char str1[20], str2[20];
  printf("Input string:");
  gets(str1);
  printf("Input another string:");
  gets(str2);
  if (MyStrcmp(str1, str2) > 0) {
    printf("str1 大于 str2\n");
  } else if (MyStrcmp(str1, str2) < 0) {
    printf("str1 小于 str2\n");
  } else {
    printf("str1 等于 str2\n");
  }
}
int MyStrcmp(char s[], char t[]) {
  int i;
  for (i = 0; s[i] == t[i]; i++)
    ;
  {
    if (s[i] = '\0')
      return 0;
  }
  return (s[i] - t[i]);
}

[Answer 11:]

#include <stdio.h>
int MyStrcmp(char s[], char t[]);
main() {
  char str1[20], str2[20];
  printf("Input string:");
  gets(str1);
  printf("Input another string:");
  gets(str2);
  if (MyStrcmp(str1, str2) > 0) {
    printf("str1 大于 str2\n");
  } else if (MyStrcmp(str1, str2) < 0) {
    printf("str1 小于 str2\n");
  } else {
    printf("str1 等于 str2\n");
  }
}
int MyStrcmp(char s[], char t[]) {
  int i;
  for (i = 0; s[i] == t[i]; i++) {
    if (s[i] == '\0')
      return 0;
  }
  return (s[i] - t[i]);
}

[Answer 12:]

#include <stdio.h>
int MyStrcmp(char s[], char t[]);
main() {
  char str1[20], str2[20];

  printf("Input string:");
  gets(str1);
  printf("Input another string:");
  gets(str2);

  if (MyStrcmp(str1, str2) > 0) {
    printf("str1 大于 str2\n");
  } else if (MyStrcmp(str1, str2) < 0) {
    printf("str1 小于 str2\n");
  } else {
    printf("str1 等于 str2\n");
  }
}

int MyStrcmp(char s[], char t[]) {
  int i;

  for (i = 0; s[i] == t[i]; i++)
    ;
  {
    if (s[i] = '\0')
      return 0;
  }
  return (s[i] - t[i]);
}

[Answer 13:]

#include <String.h>
#include <stdio.h>
int MyStrcmp(char s[], char t[]);
main() {
  char str1[20], str2[20];
  printf("Input string:");
  gets(str1);
  printf("Input another string:");
  gets(str2);
  if (MyStrcmp(str1, str2) > 0) {
    printf("str1 大于 str2\n");
  } else if (MyStrcmp(str1, str2) < 0) {
    printf("str1 小于 str2\n");
  } else {
    printf("str1 等于 str2\n");
  }
}
int MyStrcmp(char s[], char t[]) {
  int i;
  for (i = 0; s[i] == t[i]; i++)
    ;
  {
    if (s[i] == '\0')
      return 0;
  }
  return (s[i] - t[i]);
}

[Answer 14:]

#include <stdio.h>
int MyStrcmp(char s[], char t[]);
main() {
  char str1[20], str2[20];

  printf("Input string:");
  gets(str1);
  printf("Input another string:");
  gets(str2);

  if (MyStrcmp(str1, str2) > 0) {
    printf("str1 大于 str2\n");
  } else if (MyStrcmp(str1, str2) < 0) {
    printf("str1 小于 str2\n");
  } else {
    printf("str1 等于 str2\n");
  }
}

int MyStrcmp(char s[], char t[]) {
  int i;

  for (i = 0; s[i] == t[i]; i++)
    ;
  {
    if (s[i] == '\0')
      return 0;
  }
  return (s[i] - t[i]);
}

[Answer 15:]

#include <stdio.h>
int MyStrcmp(char s[], char t[]);
main() {
  char str1[20], str2[20];

  printf("Input string:");
  gets(str1);
  printf("Input another string:");
  gets(str2);

  if (MyStrcmp(str1, str2) > 0) {
    printf("str1 大于 str2\n");
  } else if (MyStrcmp(str1, str2) < 0) {
    printf("str1 小于 str2\n");
  } else {
    printf("str1 等于 str2\n");
  }
}

int MyStrcmp(char s[], char t[]) {
  int i;

  for (i = 0; s[i] == t[i]; i++)
    ;
  {
    if (s[i] == '\0')
      return 0;
  }
  return (s[i] - t[i]);
}

[Answer 16:]

#include <stdio.h>

int MyStrcmp(char s[], char t[]);

main() {
  char str1[20], str2[20];
  printf("Input string:");
  gets(str1);
  printf("Input another string:");
  gets(str2);

  if (MyStrcmp(str1, str2) > 0) {
    printf("str1 大于 str2\n");
  } else if (MyStrcmp(str1, str2) < 0) {
    printf("str1 小于 str2\n");
  } else {
    printf("str1 等于 str2\n");
  }
}

int MyStrcmp(char s[], char t[]) {
  int i;

  for (i = 0; s[i] == t[i]; i++)
    ;
  {
    if (s[i] == '\0')
      return 0;
  }
  i = (s[i] - t[i]);
  return i;
}

[Answer 17:]

#include <stdio.h>
int MyStrcmp(char s[], char t[]);
main() {
  char str1[20], str2[20];
  printf("Input string:");
  gets(str1);
  printf("Input another string:");
  gets(str2);
  if (MyStrcmp(str1, str2) > 0) {
    printf("str1 大于 str2\n");
  } else if (MyStrcmp(str1, str2) < 0) {
    printf("str1 小于 str2\n");
  } else {
    printf("str1 等于 str2\n");
  }
}
int MyStrcmp(char s[], char t[]) {
  int i;
  for (i = 0; s[i] == t[i]; i++)
    ;
  {
    if (s[i] = '\0')
      return 0;
  }
  return (s[i] - t[i]);
}

[Answer 18:]

#include <stdio.h>
int MyStrcmp(char s[], char t[]);
main() {
  char str1[20], str2[20];
  printf("Input string:");
  gets(str1);
  printf("Input another string:");
  gets(str2);
  MyStrcmp(str1, str2);
  if (MyStrcmp(str1, str2) > 0) {
    printf("str1 大于 str2\n");
  } else if (MyStrcmp(str1, str2) < 0) {
    printf("str1 小于 str2\n");
  } else {
    printf("str1 等于 str2\n");
  }
}

int MyStrcmp(char s[], char t[]) {
  int i = 0, m;
  while (s[i] != '\0') {
    m = s[i] - t[i];
    if (m > 0) {
      m = 1;
      break;
    }
    if (m < 0) {
      m = -1;
      break;
    }
    if (m == 0) {
      m = 0;
    }
    i++;
  }
  return m;
}

[Answer 19:]

#include <stdio.h>
int MyStrcmp(char s, char t);
main() {
  char str1[20], str2[20];

  printf("Input string:");
  gets(str1);
  printf("Input another string:");
  gets(str2);

  if (MyStrcmp(str1[0], str2[0]) > 0) {
    printf("str1 大于 str2\n");
  } else if (MyStrcmp(str1[0], str2[0]) < 0) {
    printf("str1 小于 str2\n");
  } else {
    printf("str1 等于 str2\n");
  }
}

int MyStrcmp(char s, char t) { return s - t; }

[Answer 20:]

#include <stdio.h>
#include <string.h>
int MyStrcmp(char s[], char t[]);
main() {
  char str1[20], str2[20];

  printf("Input string:");
  gets(str1);
  printf("Input another string:");
  gets(str2);

  if (MyStrcmp(str1, str2) > 0) {
    printf("str1 大于 str2\n");
  } else if (MyStrcmp(str1, str2) < 0) {
    printf("str1 小于 str2\n");
  } else {
    printf("str1 等于 str2\n");
  }
}

int MyStrcmp(char s[], char t[]) {
  int i;

  for (i = 0; s[i] == t[i]; i++)
    ;
  {
    if (s[i] = '\0')
      return 0;
  }
  return (s[i] - t[i]);
}

[Answer 21:]

#include <stdio.h>
int MyStrcmp(char s[], char t[]);
int main() {
  char str1[20], str2[20];
  printf("Input string:");
  gets(str1);
  printf("Input another string:");
  gets(str2);
  if (MyStrcmp(str1, str2) > 0) {
    printf("str1 大于 str2\n");
  } else if (MyStrcmp(str1, str2) < 0) {
    printf("str1 小于 str2\n");
  } else {
    printf("str1 等于 str2\n");
  }
  return 0;
}
int MyStrcmp(char s[], char t[]) {
  int i;
  for (i = 0; s[i] == t[i]; i++)
    ;
  {
    if (s[i] == '\0')
      return 0;
  }
  return (s[i] - t[i]);
}

[Answer 22:]

#include <stdio.h>
int MyStrcmp(char s[], char t[]);
main() {
  char str1[20], str2[20];
  printf("Input string:");
  gets(str1);
  printf("Input another string:");
  gets(str2);
  if (MyStrcmp(str1, str2) > 0) {
    printf("str1 大于 str2\n");
  } else if (MyStrcmp(str1, str2) < 0) {
    printf("str1 小于 str2\n");
  } else {
    printf("str1 等于 str2\n");
  }
}
int MyStrcmp(char *s, char *t) {
  int i;
  for (i = 0; s[i] == t[i]; i++)
    ;
  {
    if (s[i] == '\0' || t[i] == '0')
      return 0;
  }
  return (s[i] - t[i]);
}

[Answer 23:]

#include <stdio.h>
int MyStrcmp(char s[], char t[]);
int main() {
  char str1[20], str2[20];

  printf("Input string:");
  gets(str1);
  printf("Input another string:");
  gets(str2);

  if (MyStrcmp(str1, str2) > 0) {
    printf("str1 大于 str2\n");
  } else if (MyStrcmp(str1, str2) < 0) {
    printf("str1 小于 str2\n");
  } else {
    printf("str1 等于 str2\n");
  }
  return 0;
}

int MyStrcmp(char s[], char t[]) {
  int i;

  for (i = 0; s[i] != '\0'; i++) {
    if (s[i] != t[i]) {
      return (s[i] - t[i]);
    }
  }
  return 0;
}

[Answer 24:]

#include <stdio.h>
int MyStrcmp(char s[], char t[]);
int main() {
  char str1[20], str2[20];

  printf("Input string:");
  gets(str1);
  printf("Input another string:");
  gets(str2);

  if (MyStrcmp(str1, str2) > 0) {
    printf("str1 大于 str2\n");
  } else if (MyStrcmp(str1, str2) < 0) {
    printf("str1 小于 str2\n");
  } else {
    printf("str1 等于 str2\n");
  }
  return 0;
}

int MyStrcmp(char s[], char t[]) {
  int i;

  for (i = 0; s[i] != '\0' && s[i] == t[i]; i++)
    ;

  return (s[i] - t[i]);
}

[Answer 25:]

#include <stdio.h>
int MyStrcmp(char s[], char t[]);
main() {
  char str1[20], str2[20];
  printf("Input string:");
  gets(str1);
  printf("Input another string:");
  gets(str2);
  if (MyStrcmp(str1, str2) > 0) {
    printf("str1 大于 str2\n");
  } else if (MyStrcmp(str1, str2) < 0) {
    printf("str1 小于 str2\n");
  } else {
    printf("str1 等于 str2\n");
  }
}
int MyStrcmp(char s[], char t[]) {
  int i;
  for (i = 0; s[i] == t[i]; i++)
    ;
  {
    if (s[i] = '\0')
      return 0;
  }
  return (s[i] - t[i]);
}

[Answer 26:]

#include <stdio.h>
int MyStrcmp(char s[], char t[]);
main() {
  char str1[20], str2[20];

  printf("Input string:");
  gets(str1);
  printf("Input another string:");
  gets(str2);

  if (MyStrcmp(str1, str2) > 0) {
    printf("str1 大于 str2\n");
  } else if (MyStrcmp(str1, str2) < 0) {
    printf("str1 小于 str2\n");
  } else {
    printf("str1 等于 str2\n");
  }
}

int MyStrcmp(char s[], char t[]) {
  int i;

  for (i = 0; s[i] == t[i]; i++)
    ;
  {
    if (s[i] = '\0')
      return 0;
  }
  return (s[i] - t[i]);
}

[Answer 27:]

#include <stdio.h>
int MyStrcmp(char s[], char t[]);
main() {
  char str1[20], str2[20];

  printf("Input string:");
  gets(str1);
  printf("Input another string:");
  gets(str2);

  if (MyStrcmp(str1, str2) > 0) {
    printf("str1 大于 str2\n");
  } else if (MyStrcmp(str1, str2) < 0) {
    printf("str1 小于 str2\n");
  } else {
    printf("str1 等于 str2\n");
  }
}

int MyStrcmp(char s[], char t[]) {
  int i;

  for (i = 0; s[i] == t[i]; i++)
    ;
  {
    if (s[i] = '\0')
      return 0;
  }
  return (s[i] - t[i]);
}

[Answer 28:]

#include <stdio.h>
int MyStrcmp(char s[], char t[]);
main() {
  char str1[20], str2[20];

  printf("Input string:");
  gets(str1);
  printf("Input another string:");
  gets(str2);

  if (MyStrcmp(str1, str2) > 0) {
    printf("str1 大于 str2\n");
  } else if (MyStrcmp(str1, str2) < 0) {
    printf("str1 小于 str2\n");
  } else {
    printf("str1 等于 str2\n");
  }
}

int MyStrcmp(char s[], char t[]) {
  int i;

  for (i = 0; s[i] == t[i]; i++)
    ;
  {
    if (s[i] = '\0')
      return 0;
  }
  return (s[i] - t[i]);
}

[Answer 29:]

#include <stdio.h>
int MyStrcmp(char s[], char t[]);

main() {
  char str1[20], str2[20];

  printf("Input string:");
  gets(str1);
  printf("Input another string:");
  gets(str2);

  if (MyStrcmp(str1, str2) > 0) {
    printf("str1 大于 str2\n");
  } else if (MyStrcmp(str1, str2) < 0) {
    printf("str1 小于 str2\n");
  } else {
    printf("str1 等于 str2\n");
  }
}

int MyStrcmp(char s[], char t[]) {
  int i;

  for (i = 0; s[i] == t[i]; i++)
    ;
  {
    if (s[i] == '\0')
      return 0;
  }
  return (s[i] - t[i]);
}

[Answer 30:]

#include <stdio.h>
int MyStrcmp(char s[], char t[]);
main() {
  char str1[20], str2[20];

  printf("Input string:");
  gets(str1);
  printf("Input another string:");
  gets(str2);

  if (MyStrcmp(str1, str2) > 0) {
    printf("str1 大于 str2\n");
  } else if (MyStrcmp(str1, str2) < 0) {
    printf("str1 小于 str2\n");
  } else {
    printf("str1 等于 str2\n");
  }
}

int MyStrcmp(char s[], char t[]) {
  int i;

  for (i = 0; s[i] == t[i]; i++) {
    if (s[i] == '\0')
      return 0;
  }
  return (s[i] - t[i]);
}

[Answer 31:]

#include <stdio.h>
int MyStrcmp(char s[], char t[]);
main() {
  char str1[20], str2[20];

  printf("Input string:");
  gets(str1);
  printf("Input another string:");
  gets(str2);

  if (MyStrcmp(str1, str2) > 0) {
    printf("str1 大于 str2\n");
  } else if (MyStrcmp(str1, str2) < 0) {
    printf("str1 小于 str2\n");
  } else {
    printf("str1 等于 str2\n");
  }
}

int MyStrcmp(char s[], char t[]) {
  int i;

  for (i = 0; s[i] == t[i]; i++)
    ;
  {
    if (s[i] = '\0')
      return 0;
  }
  return (s[i] - t[i]);
}

[Answer 32:]

#include <stdio.h>
#include <string.h>
int MyStrcmp(char s[], char t[]);
int main() {
  char str1[20], str2[20];
  printf("Input string:");
  gets(str1);
  printf("Input another string:");
  gets(str2);

  if (MyStrcmp(str1, str2) > 0) {
    printf("str1 大于 str2\n");
  }
  if (MyStrcmp(str1, str2) < 0) {
    printf("str1 小于 str2\n");
  }
  if (MyStrcmp(str1, str2) == 0) {
    printf("str1 等于 str2\n");
  }
  return 0;
}

int MyStrcmp(char s[], char t[]) {
  int i;
  for (i = 0; s[i] == t[i]; i++)
    ;
  {
    if ((s[i] == '\0') || (t[i] == '\0'))
      return 0;
  }
  return (s[i] - t[i]);
}

[Answer 33:]

#include <stdio.h>
int MyStrcmp(char s[], char t[]);
main() {
  char str1[20], str2[20];

  printf("Input string:");
  gets(str1);
  printf("Input another string:");
  gets(str2);

  if (MyStrcmp(str1, str2) > 0) {
    printf("str1 大于 str2\n");
  } else if (MyStrcmp(str1, str2) < 0) {
    printf("str1 小于 str2\n");
  } else {
    printf("str1 等于 str2\n");
  }
}

int MyStrcmp(char s[], char t[]) {
  int i;

  for (i = 0; s[i] == t[i]; i++)
    ;
  {
    if (s[i] = '\0' || t[i] == '\0')
      return 0;
  }
  return (s[i] - t[i]);
}

[Answer 34:]

#include <stdio.h>
int MyStrcmp(char s[], char t[]);
int main() {
  char str1[20], str2[20];
  printf("Input string:");
  gets(str1);
  printf("Input another string:");
  gets(str2);
  if (MyStrcmp(str1, str2) > 0) {
    printf("str1 大于 str2\n");
  } else if (MyStrcmp(str1, str2) < 0) {
    printf("str1 小于 str2\n");
  } else {
    printf("str1 等于 str2\n");
  }
  return 0;
}
int MyStrcmp(char s[], char t[]) {
  int i;
  for (i = 0; s[i] == t[i]; i++)
    ;
  {
    if (s[i] == '\0')
      return 0;
  }
  return (s[i] - t[i]);
}

[Answer 35:]

#include <stdio.h>
#include <string.h>
#define N 80
int MyStrcmp(char s[], char t[]);
main() {
  int ret;
  char s[N], t[N];
  printf("Input string:");
  gets(s);
  printf("Input another string:");
  gets(t);
  ret = MyStrcmp(s, t);
  if (ret == 1) {
    printf("str1 大于 str2\n");
  } else if (ret == -1) {
    printf("str1 小于 str2\n");
  } else if (ret == 0) {
    printf("str1 等于 str2\n");
  }
}
int MyStrcmp(char s[], char t[]) {
  int k;
  k = strcmp(s, t);
  if (k > 0) {
    return 1;
  } else if (k < 0) {
    return -1;
  } else if (k == 0) {
    return 0;
  }
}

[Answer 36:]

#include <stdio.h>
int MyStrcmp(char s[], char t[]);
main() {
  char str1[20], str2[20];

  printf("Input string:");
  gets(str1);
  printf("Input another string:");
  gets(str2);

  if (MyStrcmp(str1, str2) > 0) {
    printf("str1 小于 str2\n");
  } else if (MyStrcmp(str1, str2) < 0) {
    printf("str1 大于 str2\n");
  } else {
    printf("str1 等于 str2\n");
  }
}

int MyStrcmp(char s[], char t[])

{

  int i;

  for (i = 0; s[i] == t[i]; i++)

  {

    return (s[i] - t[i]);
  }
}

[Answer 37:]

#include <stdio.h>
int MyStrcmp(char s[], char t[]);
main() {
  char str1[20], str2[20];

  printf("Input string:");
  gets(str1);
  printf("Input another string:");
  gets(str2);

  if (MyStrcmp(str1, str2) > 0) {
    printf("str1 大于 str2\n");
  } else if (MyStrcmp(str1, str2) < 0) {
    printf("str1 小于 str2\n");
  } else {
    printf("str1 等于 str2\n");
  }
}

int MyStrcmp(char s[], char t[]) {
  int i;

  for (i = 0; s[i] == t[i]; i++)
    ;
  {
    if (s[i] = '\0')
      return 0;
  }
  return (s[i] - t[i]);
}

[Answer 38:]

#include <stdio.h>
int MyStrcmp(char s[], char t[]);
main() {
  char str1[20], str2[20];
  printf("Input string:");
  gets(str1);
  printf("Input another string:");
  gets(str2);
  if (MyStrcmp(str1, str2) > 0)
    printf("str1 大于 str2\n");
  else if (MyStrcmp(str1, str2) < 0)
    printf("str1 小于 str2\n");
  else
    printf("str1 等于 str2\n");
}
int MyStrcmp(char s[], char t[]) {
  int i;
  for (i = 0; s[i] == t[i]; i++)
    if (s[i] == '\0')
      return 0;
  return (s[i] - t[i]);
}

[Answer 39:]

#include <stdio.h>
#define SIZE 80
intMyStrcmp(char s[], char t[]);
main() {
  char s[SIZE], t[SIZE], i;
  printf("Input string:");
  gets(s); // 1
  printf("Input another string:");
  gets(t);            // 1
  i = MyStrcmp(s, t); // 2
  if (i > 0)          // 1
    printf("str1 大于 str2\n");
  else if (i < 0) // 1
    printf("str1 小于 str2\n");
  else // 1
    printf("str1 等于 str2\n");
}
int MyStrcmp(char s[], char t[]) {
  int i;
  for (i = 0; s[i] == t[i]; i++) // 6
  {
    if (s[i] == '\0') // 2
      return 0;       // 2
  }
  return (s[i] - t[i]); // 2
}

[Answer 40:]

#include <stdio.h>
int MyStrcmp(char s[], char t[]);
main() {
  char str1[20], str2[20];

  printf("Input string:");
  gets(str1);
  printf("Input another string:");
  gets(str2);

  if (MyStrcmp(str1, str2) > 0) {
    printf("str1 大于 str2\n");
  } else if (MyStrcmp(str1, str2) < 0) {
    printf("str1 小于 str2\n");
  } else {
    printf("str1 等于 str2\n");
  }
}

int MyStrcmp(char s[], char t[]) {
  int i;

  for (i = 0; s[i] == t[i]; i++)
    ;
  {
    if (s[i] == '\0')
      return 0;
  }
  return (s[i] - t[i]);
}

[Answer 41:]

#include <stdio.h>
int MyStrcmp(char s[], char t[]);
main() {
  char str1[20], str2[20];

  printf("Input string:");
  gets(str1);
  printf("Input another string:");
  gets(str2);
  if (MyStrcmp(str1, str2) > 0) {
    printf("str1 大于 str2\n");
  } else if (MyStrcmp(str1, str2) < 0) {
    printf("str1 小于 str2\n");
  } else {
    printf("str1 等于 str2\n");
  }
}

int MyStrcmp(char s[], char t[]) {
  int i;

  for (i = 0; s[i] == t[i]; i++) {
    if (s[i] == '\0' && t[i] == '\0')
      return 0;
  }
  return (s[i] - t[i]);
}

[Answer 42:]

#include <stdio.h>
int MyStrcmp(char s[], char t[]);
main() {
  char str1[20], str2[20];
  printf("Input string:");
  gets(str1);
  printf("Input another string:");
  gets(str2);
  if (MyStrcmp(str1, str2) > 0) {
    printf("str1 大于 str2\n");
  } else if (MyStrcmp(str1, str2) < 0) {
    printf("str1 小于 str2\n");
  } else {
    printf("str1 等于 str2\n");
  }
}
int MyStrcmp(char s[], char t[]) {
  int i;
  for (i = 0; s[i] == t[i]; i++)
    ;
  {
    if (s[i] == '\0')
      return 0;
  }
  return (s[i] - t[i]);
}

[Answer 43:]

#include <stdio.h>
int MyStrcmp(char s[], char t[]);
main() {
  char str1[20], str2[20];
  printf("Input string:");
  gets(str1);
  printf("Input another string:");
  gets(str2);
  if (MyStrcmp(str1, str2) > 0) {
    printf("str1 大于 str2\n");
  } else if (MyStrcmp(str1, str2) < 0) {
    printf("str1 小于 str2\n");
  } else {
    printf("str1 等于 str2\n");
  }
}
int MyStrcmp(char s[], char t[]) {
  int i;
  for (i = 0; s[i] == t[i]; i++)
    ;
  {
    if (s[i] == '\0')
      return 0;
  }
  return (s[i] - t[i]);
}

[Answer 44:]

#include <stdio.h>
int MyStrcmp(char s[], char t[]);
main() {
  char str1[20], str2[20];

  printf("Input string:");
  gets(str1);
  printf("Input another string:");
  gets(str2);

  if (MyStrcmp(str1, str2) > 0) {
    printf("str1 大于 str2\n");
  } else if (MyStrcmp(str1, str2) < 0) {
    printf("str1 小于 str2\n");
  } else {
    printf("str1 等于 str2\n");
  }
}

int MyStrcmp(char s[], char t[]) {
  int i;

  for (i = 0; s[i] == t[i]; i++)
    ;
  {
    if (s[i] == '\0' && t[i] == '\0')
      return 0;
    if (s[i] == '\0' && t[i] != '\0')
      return -1;
  }
  return (s[i] - t[i]);
}

[Answer 45:]

#include <stdio.h>
int MyStrcmp(char s[], char t[]);
main() {
  char str1[20], str2[20];
  printf("Input string:");
  gets(str1);
  printf("Input another string:");
  gets(str2);
  if (MyStrcmp(str1, str2) > 0) {
    printf("str1 大于 str2\n");
  } else if (MyStrcmp(str1, str2) < 0) {
    printf("str1 小于 str2\n");
  } else {
    printf("str1 等于 str2\n");
  }
}
int MyStrcmp(char s[], char t[]) {
  int i;
  for (i = 0; i < 20; i++)
    ;
  {
    if (s[i] = '\0')
      return 0;
    return (s[i] - t[i]);
  }
}

[Answer 46:]

#include <stdio.h>
int MyStrcmp(char s[], char t[]);
main() {
  char str1[20], str2[20];
  printf("Input string:");
  gets(str1);
  printf("Input another string:");
  gets(str2);
  if (MyStrcmp(str1, str2) > 0) {
    printf("str1 大于 str2\n");
  } else if (MyStrcmp(str1, str2) < 0) {
    printf("str1 小于 str2\n");
  } else {
    printf("str1 等于 str2\n");
  }
}

int MyStrcmp(char s[], char t[]) {
  int i;

  for (i = 0; s[i] == t[i]; i++) {
    if (s[i] == '\0')
      return 0;
  }
  return (s[i] - t[i]);
}

[Answer 47:]

#include <stdio.h>
int MyStrcmp(char s[], char t[]);
main() {
  char str1[20], str2[20];
  printf("Input string:");
  gets(str1);
  printf("Input another string:");
  gets(str2);
  if (MyStrcmp(str1, str2) > 0) {
    printf("str1 大于 str2\n");
  } else if (MyStrcmp(str1, str2) < 0) {
    printf("str1 小于 str2\n");
  } else {
    printf("str1 等于 str2\n");
  }
}

int MyStrcmp(char s[], char t[]) {
  int i;

  for (i = 0; s[i] == t[i]; i++) {
    if (s[i] == '\0')
      return 0;
  }
  return (s[i] - t[i]);
}

[Answer 48:]

#include <stdio.h>
int MyStrcmp(char s[], char t[]);
main() {
  char str1[20], str2[20];
  printf("Input string:");
  gets(str1);
  printf("Input another string:");
  gets(str2);

  if (MyStrcmp(str1, str2) > 0) {
    printf("str1 大于 str2\n");
  } else if (MyStrcmp(str1, str2) < 0) {
    printf("str1 小于 str2\n");
  } else {
    printf("str1 等于 str2\n");
  }
}

int MyStrcmp(char s[], char t[]) {
  int i;
  for (i = 0; s[i] == t[i]; i++) {
    if (s[i] == '\0')
      return 0;
  }
  return (s[i] - t[i]);
}

[Answer 49:]

#include <stdio.h>
int MyStrcmp(char s[], char t[]);
main() {
  char str1[20], str2[20];
  printf("Input string:");
  gets(str1);
  printf("Input another string:");
  gets(str2);
  if (MyStrcmp(str1, str2) > 0) {
    printf("str1 大于 str2\n");
  } else if (MyStrcmp(str1, str2) < 0) {
    printf("str1 小于 str2\n");
  } else {
    printf("str1 等于 str2\n");
  }
}
int MyStrcmp(char s[], char t[]) {
  int i;
  for (i = 0; s[i] == t[i]; i++) {
    if (s[i] = '\0')
      return 0;
  }
  return (s[i] - t[i]);
}

[Answer 50:]

#include <stdio.h>
int MyStrcmp(char s[], char t[]);
int main() {
  char str1[20], str2[20];
  printf("Input string:");
  gets(str1);
  printf("Input another string:");
  gets(str2);
  if (MyStrcmp(str1, str2) > 0) {
    printf("str1 大于 str2\n");
  } else if (MyStrcmp(str1, str2) < 0) {
    printf("str1 小于 str2\n");
  } else {
    printf("str1 等于 str2\n");
  }
  return 0;
}

int MyStrcmp(char s[], char t[]) {
  int i;
  for (i = 0; s[i] == t[i]; i++)
    ;
  {
    if (s[i] == '\0')
      return 0;
  }
  return (s[i] - t[i]);
}

[Answer 51:]

#include <stdio.h>
int MyStrcmp(char s[], char t[]);
main() {
  char str1[20], str2[20];

  printf("Input string:");
  gets(str1);
  printf("Input another string:");
  gets(str2);

  if (MyStrcmp(str1, str2) > 0) {
    printf("str1 大于 str2\n");
  } else if (MyStrcmp(str1, str2) < 0) {
    printf("str1 小于 str2\n");
  } else {
    printf("str1 等于 str2\n");
  }
}

int MyStrcmp(char s[], char t[]) {
  int i;

  for (i = 0; s[i] == t[i]; i++)
    ;
  {
    if (s[i] = '\0')
      return 0;
  }
  return (s[i] - t[i]);
}

[Answer 52:]

#include <stdio.h>
#include <string.h>
int MyStrcmp(char s[], char t[]);
main() {
  char str1[20], str2[20];

  printf("Input string:");
  gets(str1);
  printf("Input another string:");
  gets(str2);

  if (MyStrcmp(str1, str2) > 0) {
    printf("str1 大于 str2\n");
  } else if (MyStrcmp(str1, str2) < 0) {
    printf("str1 小于 str2\n");
  } else {
    printf("str1 等于 str2\n");
  }
}

int MyStrcmp(char s[], char t[]) {
  int i;

  for (i = 0; s[i] == t[i]; i++) {
    if (s[i] == '\0')
      return 0;
  }
  return (s[i] - t[i]);
}

[Answer 53:]

#include <stdio.h>
int MyStrcmp(char s[], char t[]);
main() {
  char str1[20], str2[20];

  printf("Input string:");
  gets(str1);
  printf("Input another string:");
  gets(str2);

  if (MyStrcmp(str1, str2) > 0) {
    printf("str1 大于 str2\n");
  } else if (MyStrcmp(str1, str2) < 0) {
    printf("str1 小于 str2\n");
  } else {
    printf("str1 等于 str2\n");
  }
}

int MyStrcmp(char s[], char t[]) {
  int i;

  for (i = 0; s[i] == t[i]; i++)
    ;
  {
    if (s[i] == '\0')
      return 0;
  }
  return (s[i] - t[i]);
}

[Answer 54:]

#include <stdio.h>
#include <string.h>
int MyStrcmp(char s[], char t[]);
main() {
  char str1[20], str2[20];

  printf("Input string:");
  gets(str1);
  printf("Input another string:");
  gets(str2);

  if (MyStrcmp(str1, str2) > 0) {
    printf("str1 大于 str2\n");
  } else if (MyStrcmp(str1, str2) < 0) {
    printf("str1 小于 str2\n");
  } else {
    printf("str1 等于 str2\n");
  }
}

int MyStrcmp(char s[], char t[]) {
  int i;

  for (i = 0; s[i] == t[i]; i++)
    ;
  {
    if (s[i] == '\0')
      return 0;
  }
  return (s[i] - t[i]);
}

[Answer 55:]

#include <stdio.h>
int MyStrcmp(char s[], char t[]);
main() {
  char str1[20], str2[20];

  printf("Input string:");
  gets(str1);
  printf("Input another string:");
  gets(str2);

  if (MyStrcmp(str1, str2) > 0) {
    printf("str1 大于 str2\n");
  } else if (MyStrcmp(str1, str2) < 0) {
    printf("str1 小于 str2\n");
  } else {
    printf("str1 等于 str2\n");
  }
}

int MyStrcmp(char s[], char t[]) {
  int i;

  for (i = 0; s[i] == t[i]; i++)
    ;
  {
    if (s[i] = '\0')
      return 0;
  }
  return (s[i + 1] - t[i + 1]);
}

[Answer 56:]

#include <stdio.h>
#include <string.h>
int MyStrcmp(char s[], char t[]);
main() {
  char str1[20], str2[20];

  printf("Input string:");
  gets(str1);
  printf("Input another string:");
  gets(str2);

  if (MyStrcmp(str1, str2) > 0) {
    printf("str1 大于 str2\n");
  } else if (MyStrcmp(str1, str2) < 0) {
    printf("str1 小于 str2\n");
  } else {
    printf("str1 等于 str2\n");
  }
}

int MyStrcmp(char s[], char t[]) {
  int i;

  for (i = 0; s[i] == t[i]; i++)
    ;
  {
    if (s[i] == '\0')
      return 0;
  }
  return (s[i] - t[i]);
}

[Answer 57:]

#include <stdio.h>
int MyStrcmp(char s[], char t[]);
int main() {
  char str1[20], str2[20];

  printf("Input string:");
  gets(str1);
  printf("Input another string:");
  gets(str2);

  if (MyStrcmp(str1, str2) > 0) {
    printf("str1 大于 str2\n");
  } else if (MyStrcmp(str1, str2) < 0) {
    printf("str1 小于 str2\n");
  } else {
    printf("str1 等于 str2\n");
  }
  return 0;
}

int MyStrcmp(char s[], char t[]) {
  int i;

  for (i = 0; s[i] == t[i]; i++)
    ;
  {
    if (s[i] = '\0')
      return -1;
  }
  return (s[i] - t[i]);
}

[Answer 58:]

#include <stdio.h>
int MyStrcmp(char s[], char t[]);
main() {
  char str1[20], str2[20];
  printf("Input string:");
  gets(str1);
  printf("Input another string:");
  gets(str2);
  if (MyStrcmp(str1, str2) > 0) {
    printf("str1 大于 str2\n");
  } else if (MyStrcmp(str1, str2) < 0) {
    printf("str1 小于 str2\n");
  } else {
    printf("str1 等于 str2\n");
  }
}
int MyStrcmp(char s[], char t[]) {
  int i;
  for (i = 0; s[i] == t[i]; i++) {
    if (s[i] == '\0')
      return 0;
  }
  return (s[i] - t[i]);
}

[Answer 59:]

#include <stdio.h>
int MyStrcmp(char s[], char t[]);
int main() {
  char str1[20], str2[20];

  printf("Input string:");
  gets(str1);
  printf("Input another string:");
  gets(str2);

  if (MyStrcmp(str1, str2) > 0) {
    printf("str1 大于 str2\n");
  } else if (MyStrcmp(str1, str2) < 0) {
    printf("str1 小于 str2\n");
  } else {
    printf("str1 等于 str2\n");
  }
}
int MyStrcmp(char s[], char t[]) {
  int i;

  for (i = 0; s[i] == t[i]; i++) {
    if (s[i] == '\0')
      return 0;
  }
  return (s[i] - t[i]);
}

[Answer 60:]

#include <stdio.h>
int MyStrcmp(char s[], char t[]);
main() {
  char str1[20], str2[20];
  printf("Input string:");
  gets(str1);
  printf("Input another string:");
  gets(str2);
  if (MyStrcmp(str1, str2) > 0) {
    printf("str1 大于 str2\n");
  } else if (MyStrcmp(str1, str2) < 0) {
    printf("str1 小于 str2\n");
  } else {
    printf("str1 等于 str2\n");
  }
}
int MyStrcmp(char s[], char t[]) {
  int i;
  for (i = 0; s[i] == t[i] && s[i] != '\0'; i++) {
    if (s[i] == '\0')
      return 0;
  }
  return (s[i] - t[i]);
}

[Answer 61:]

#include <stdio.h>
int MyStrcmp(char s[], char t[]);
main() {
  char str1[20], str2[20];

  printf("Input string:");
  gets(str1);
  printf("Input another string:");
  gets(str2);

  if (MyStrcmp(str1, str2) > 0) {
    printf("str1 大于 str2\n");
  } else if (MyStrcmp(str1, str2) < 0) {
    printf("str1 小于 str2\n");
  } else {
    printf("str1 等于 str2\n");
  }
}

int MyStrcmp(char s[], char t[]) {
  int i;

  for (i = 0; s[i] == t[i]; i++)
    ;
  {
    if (s[i] == '\0')
      return 0;
  }
  return (s[i] - t[i]);
}

[Answer 62:]

#include <stdio.h>
int MyStrcmp(char s[], char t[]);
main() {
  char str1[20], str2[20];

  printf("Input string:");
  gets(str1);
  printf("Input another string:");
  gets(str2);

  if (MyStrcmp(str1, str2) > 0) {
    printf("str1 大于 str2\n");
  } else {
    if (MyStrcmp(str1, str2) < 0) {
      printf("str1 小于 str2\n");
    } else {
      printf("str1 等于 str2\n");
    }
  }
}

int MyStrcmp(char s[], char t[]) {
  int i;

  for (i = 0; s[i] == t[i]; i++) {
    if (s[i] == '\0')
      return 0;
  }
  return (s[i] - t[i]);
}

[Answer 63:]

#include <stdio.h>
int MyStrcmp(char s[], char t[]);
int main() {
  char str1[20], str2[20];
  printf("Input string:");
  gets(str1);
  printf("Input another string:");
  gets(str2);
  if (MyStrcmp(str1, str2) > 0) {
    printf("str1 大于 str2\n");
  } else if (MyStrcmp(str1, str2) < 0) {
    printf("str1 小于 str2\n");
  } else {
    printf("str1 等于 str2\n");
  }
  return 0;
}
int MyStrcmp(char s[], char t[]) {
  int i;
  for (i = 0; s[i] == t[i]; i++)
    ;
  {
    if (s[i] == '\0' || t[i] == '\0')
      return 0;
  }
  return (s[i] - t[i]);
}

[Answer 64:]

#include <stdio.h>
int MyStrcmp(char s[], char t[]);
main() {
  char str1[20], str2[20];
  printf("Input string:");
  gets(str1);
  printf("Input another string:");
  gets(str2);
  if (MyStrcmp(str1, str2) > 0) {
    printf("str1 大于 str2\n");
  } else if (MyStrcmp(str1, str2) < 0) {
    printf("str1 小于 str2\n");
  } else {
    printf("str1 等于 str2\n");
  }
}
int MyStrcmp(char s[], char t[]) {
  int i;
  for (i = 0; s[i] == t[i] && (s[i] != 0 && t[i] != 0); i++)
    ;
  {
    if (s[i] = '\0')
      return 0;
  }
  return (s[i] - t[i]);
}

[Answer 65:]

#include <stdio.h>

int MyStrcmp(char s[], char t[]);

main() {
  char str1[20], str2[20];

  printf("Input string:");
  gets(str1);
  printf("Input another string:");
  gets(str2);

  if (MyStrcmp(str1, str2) > 0) {
    printf("str1 大于 str2\n");
  } else if (MyStrcmp(str1, str2) < 0) {
    printf("str1 小于 str2\n");
  } else {
    printf("str1 等于 str2\n");
  }
}

int MyStrcmp(char s[], char t[]) {
  int i, k;

  for (i = 0; s[i] == t[i]; i++)
    ;
  {
    if (s[i] == '\0' || t[i] == '\0')
      return 0;
  }
  k = s[i] - t[i];
  return k;
}

[Answer 66:]

#include <stdio.h>
#include <stdlib.h>
int MyStrcmp(char s[], char t[]);
int main() {
  char str1[20], str2[20];

  printf("Input string:");
  gets(str1);
  printf("Input another string:");
  gets(str2);

  if (MyStrcmp(str1, str2) > 0) {
    printf("str1 大于 str2\n");
  } else if (MyStrcmp(str1, str2) < 0) {
    printf("str1 小于 str2\n");
  } else {
    printf("str1 等于 str2\n");
  }
  return 0;
}

int MyStrcmp(char s[], char t[]) {
  int i;

  for (i = 0; s[i] == t[i]; i++)
    ;
  {
    if (s[i] = '\0')
      return 0;
  }
  return (s[i] - t[i]);
}

[Answer 67:]

#include <stdio.h>
int MyStrcmp(char s[], char t[]);
main() {
  char str1[20], str2[20];

  printf("Input string:");
  gets(str1);
  printf("Input another string:");
  gets(str2);

  if (MyStrcmp(str1, str2) > 0) {
    printf("str1 大于 str2\n");
  } else if (MyStrcmp(str1, str2) < 0) {
    printf("str1 小于 str2\n");
  } else {
    printf("str1 等于 str2\n");
  }
}

int MyStrcmp(char s[], char t[]) {
  int i;

  for (i = 0; s[i] == t[i]; i++)
    ;
  {
    if (s[i] == '\0')
      return 0;
    else if (s[i] == '\0')
      return 0;
  }
  return (s[i] - t[i]);
}

[Answer 68:]

#include <stdio.h>
int MyStrcmp(char s[], char t[]);
main() {
  char str1[20], str2[20];

  printf("Input string:");
  gets(str1);
  printf("Input another string:");
  gets(str2);

  if (MyStrcmp(str1, str2) > 0) {
    printf("str1 大于 str2\n");
  } else if (MyStrcmp(str1, str2) < 0) {
    printf("str1 小于 str2\n");
  } else {
    printf("str1 等于 str2\n");
  }
}

int MyStrcmp(char s[], char t[]) {
  int i;

  for (i = 0; s[i] == t[i]; i++)
    ;
  {
    if (s[i] = '\0')
      return 0;
  }
  return (s[i] - t[i]);
}

[Answer 69:]

#include <stdio.h>
int MyStrcmp(char s[], char t[]);
main() {
  char str1[20], str2[20];
  printf("Input string:");
  gets(str1);
  printf("Input another string:");
  gets(str2);
  if (MyStrcmp(str1, str2) > 0) {
    printf("str1 大于 str2\n");
  } else if (MyStrcmp(str1, str2) < 0) {
    printf("str1 小于 str2\n");
  } else {
    printf("str1 等于 str2\n");
  }
}
int MyStrcmp(char s[], char t[]) {
  int i;
  for (i = 0; s[i] == t[i]; i++) {
    if (s[i] == '\0') {
      return 0;
    }
  }
  return (s[i] - t[i]);
}

[Answer 70:]

#include <math.h>
#include <stdio.h>
int MyStrcmp(char s[], char t[]);
main() {
  char str1[20], str2[20];

  printf("Input string:");
  gets(str1);
  printf("Input another string:");
  gets(str2);

  if (MyStrcmp(str1, str2) > 0) {
    printf("str1 大于 str2\n");
  } else if (MyStrcmp(str1, str2) < 0) {
    printf("str1 小于 str2\n");
  } else {
    printf("str1 等于 str2\n");
  }
}

int MyStrcmp(char s[], char t[]) {
  int i;

  for (i = 0; s[i] == t[i]; i++)
    ;
  {
    if (s[i] == '\0' || t[i] == '\0')
      return 0;
  }
  return (s[i - 1] - t[i - 1]);
}

[Answer 71:]

#include <stdio.h>

int MyStrcmp(char s[], char t[]);
main() {
  char str1[20], str2[20];

  printf("Input string:");
  gets(str1);
  printf("Input another string:");
  gets(str2);

  if (MyStrcmp(str1, str2) > 0) {
    printf("str1 大于 str2\n");
  } else if (MyStrcmp(str1, str2) < 0) {
    printf("str1 小于 str2\n");
  } else {
    printf("str1 等于 str2\n");
  }
}

int MyStrcmp(char s[], char t[]) {
  int i;

  for (i = 0; s[i] == t[i]; i++)
    ;
  {
    if (s[i] == '\0')
      return 0;
  }
  return (s[i] - t[i]);
}

[Answer 72:]

#include <stdio.h>
int MyStrcmp(char s[], char t[]);
main() {
  char str1[20], str2[20];

  printf("Input string:");
  gets(str1);
  printf("Input another string:");
  gets(str2);

  if (MyStrcmp(str1, str2) > 0) {
    printf("str1 大于 str2\n");
  } else if (MyStrcmp(str1, str2) < 0) {
    printf("str1 小于 str2\n");
  } else {
    printf("str1 等于 str2\n");
  }
}

int MyStrcmp(char s[], char t[]) {
  int i;

  for (i = 0; s[i] == t[i]; i++)
    ;
  {
    if (s[i] = '\0')
      return 0;
  }
  return (s[i] - t[i]);
}

[Answer 73:]

#include <stdio.h>
int MyStrcmp(char s[], char t[]);
int main() {
  char str1[20], str2[20];

  printf("Input string:");
  gets(str1);
  printf("Input another string:");
  gets(str2);

  if (MyStrcmp(str1, str2) > 0) {
    printf("str1 大于 str2\n");
  } else if (MyStrcmp(str1, str2) < 0) {
    printf("str1 小于 str2\n");
  } else {
    printf("str1 等于 str2\n");
  }
}

int MyStrcmp(char s[], char t[]) {
  int i;

  for (i = 0; s[i] == t[i]; i++) {
    if (s[i] == '\0' && t[i] == '\0')
      return 0;
  }
  return (s[i] - t[i]);
}

[Answer 74:]

#include <stdio.h>
int MyStrcmp(char s[], char t[], int n);
main() {
  char str1[20], str2[20];
  int n, b;
  scanf("%d", &n);
  getchar();
  printf("Input string:");
  gets(str1);
  printf("Input another string:");
  gets(str2);
  b = MyStrcmp(str1, str2, n);
  if (b > 0) {
    printf("str1 大于 str2\n");
  } else if (b < 0) {
    printf("str1 小于 str2\n");
  } else {
    printf("str1 等于 str2\n");
  }
}
int MyStrcmp(char s[], char t[], int n) {
  int i, a;
  for (i = 0; s[i] == t[i]; i++)
    ;
  {
    if (s[i] = '\0')
      return 0;
  }
  a = s[i] - t[i];
  return a;
}

[Answer 75:]

#include <stdio.h>
int MyStrcmp(char s[], char t[]);
main() {
  char str1[20], str2[20];
  printf("Input string:");
  gets(str1);
  printf("Input another string:");
  gets(str2);
  if (MyStrcmp(str1, str2) > 0) {
    printf("str1 大于 str2\n");
  } else if (MyStrcmp(str1, str2) < 0) {
    printf("str1 小于 str2\n");
  } else {
    printf("str1 等于 str2\n");
  }
}
int MyStrcmp(char s[], char t[]) {
  int i;
  for (i = 0; s[i] == t[i]; i++)
    ;
  {
    if (s[i] = '\0')
      return 0;
  }
  return (s[i] - t[i]);
}


[75 answers found]