[Answer 1:]

#include <math.h>
#include <stdio.h>
int main() {
  float x, y;
  printf("Please input x:\n");
  scanf("%f\n", &x);
  if (x < 0)
    y = 3 * x - 1;
  else if (x >= 0 && x < 10)
    y = exp(x);
  else
    y = x;
  printf("y = %.2f\n", y);
  return 0;
}

[Answer 2:]

#include <math.h>
#include <stdio.h>
#include <stdlib.h>
int main() {
  float x, y;
  printf("Please input x:\n");
  scanf("%f", &x);
  if (x < 0) {
    y = 3 * x - 1;
  } else if (x >= 0 && x < 10) {
    y = exp(x);
  } else {
    y = x;
  }
  printf("y = %.2f\n", y);
  return 0;
}

[Answer 3:]

#include <math.h>
#include <stdio.h>
#include <stdlib.h>
int main() {
  float x, y;
  printf("Please input x:\n");
  scanf("%f", &x);
  if (x < 0) {
    y = 3 * x - 1;
  } else if (x >= 0 && x < 10) {
    y = exp(x);
  } else {
    y = x;
  }
  printf("y = %.2f\n", y);
  return 0;
}

[Answer 4:]

#include <math.h>
#include <stdio.h>
main() {
  float x;
  float y;
  printf("Please input x:\n");
  scanf("%f", &x);
  if (x < 0)
    y = 3 * x - 1;
  if (x >= 0 && x < 10)
    y = exp(x);
  if (x >= 10)
    y = x;
  printf("y = %.2f\n", y);
}

[Answer 5:]

#include <math.h>
#include <stdio.h>
#include <stdlib.h>

int main() {
  float x, y;
  printf("Please input x:\n");
  scanf("%f", &x);
  if (x < 0) {
    y = 3 * x - 1;
  }
  if (0 <= x && x < 10) {
    y = exp(x);
  }
  if (x >= 10) {
    y = x;
  }
  printf("y = %.2f\n", y);
  return 0;
}

[Answer 6:]

#include <math.h>
#include <stdio.h>
#include <stdlib.h>

int main() {
  float x, y;
  printf("Please input x:\n");
  scanf("%f", &x);
  if (x < 0) {
    y = 3 * x - 1;
  }
  if (0 <= x && x < 10) {
    y = exp(x);
  }
  if (x >= 10) {
    y = x;
  }
  printf("y = %.2f\n", y);
  return 0;
}

[Answer 7:]

#include <math.h>
#include <stdio.h>
#include <stdlib.h>
int main() {
  float a, b, c, x, y;
  float s, ar;
  printf("Please input x:\n");
  scanf("%f", &x);
  if (x < 0) {
    y = 3 * x - 1;
    printf("y = %.2f\n", y);
  }
  if (x >= 10)
    printf("y = %.2f\n", x);
  if (x < 10 && x >= 0) {
    y = exp(x);
    printf("y = %.2f\n", y);
  }
  return 0;
}

[Answer 8:]

#include <math.h>
#include <stdio.h>

int main() {
  float x;

  printf("Please input x:\n");
  scanf("%f", &x);
  if (x < 0)
    printf("y = %.2f\n", 3 * x - 1);
  else if (x >= 10)
    printf("y = %.2f\n", x);
  else
    printf("y = %.2f\n", exp(x));

  return 0;
}

[Answer 9:]

#include <math.h>
#include <stdio.h>
#include <stdlib.h>
int main() {
  float n;
  printf("Please input x:\n");
  scanf("%f", &n);
  if (n >= 10)
    printf("y = %.2f\n", n);
  else if (n >= 0 && n < 10)
    printf("y = %.2f\n", exp(n));
  else
    printf("y = %.2f\n", 3 * n - 1);
  return 0;
}

[Answer 10:]

#include <math.h>
#include <stdio.h>
void main(void) {
  float x, y;
  printf("Please input x:\n");
  scanf("%f", &x);
  if (x < 0) {
    y = 3 * x - 1;
  } else if (x > 0 && x <= 10) {
    y = exp(x);
  } else if (x >= 10) {
    y = x;
  }
  printf("y = %.2f\n", y);
}

[Answer 11:]

#include <math.h>
#include <stdio.h>

int main() {
  float x;

  printf("Please input x:\n");
  scanf("%f", &x);

  if (x < 0) {
    printf("y = %.2f\n", 3 * x - 1);
  }
  if (x >= 0 && x < 10) {
    printf("y = %.2f\n", exp(x));
  }
  if (x >= 10) {
    printf("y = %.2f\n", x);
  }
}

[Answer 12:]

#include <math.h>
#include <stdio.h>
#include <string.h>

int main() {
  float x;
  printf("Please input x:\n");
  scanf("%f", &x);
  if (x >= 10)
    printf("y = %.2f\n", x);
  else if (x >= 0 && x < 10)
    printf("y = %.2f\n", exp(x));
  else
    printf("y = %.2f\n", 3 * x - 1);
}

[Answer 13:]

#include <stdio.h>
int main() {
  float x;
  printf("Please input x:\n");
  scanf("%f", &x);
  if (x < 0)
    printf("y = %.2f\n", 3 * x - 1);
  else if (x >= 0 && x < 10)
    printf("y = %.2f\n", exp(x));
  else
    printf("y = %.2f\n", x);
}

[Answer 14:]

#include <math.h>
#include <stdio.h>
int main() {
  float x, y;
  printf("Please input x:\n");
  scanf("%f", &x);
  if (x < 0)
    y = 3 * x - 1;
  else if (x >= 0 && x < 10)
    y = exp(x);
  else
    y = x;
  printf("y = %.2f\n", y);
  return 0;
}

[Answer 15:]

#include <math.h>
#include <stdio.h>
int main() {
  float x, y;
  printf("Please input x:\n");
  scanf("%f", &x);
  if (x < 0)
    y = 3 * x - 1;
  else if (x >= 0 && x < 10)
    y = exp(x);
  else
    y = x;
  printf("y = %.2f\n", y);
  return 0;
}


[15 answers found]