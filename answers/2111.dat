[Answer 1:]

#include <stdio.h>
int main() {
  int i, x = 0;
  int a[20];
  for (i = 0; i < 10; i++) {
    printf("input the number!\n");
    scanf("%d", &a[i]);
    if (a[i] > x)
      x = a[i];
  }
  printf("max integer is %d!\n", x);
}

[Answer 2:]

#include <stdio.h>
main() {
  int a[10];
  int max, i;
  for (i = 0; i < 10; i++) {
    printf("input the number!\n");
    scanf("%d", &a[i]);
  }
  max = a[0];
  for (i = 0; i < 10; i++) {
    if (a[i] > max)
      max = a[i];
  }
  printf("max integer is %d!\n", max);
}


[2 answers found]