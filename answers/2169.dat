[Answer 1:]

#include <stdio.h>
int main() {
  int a, b;
  char c;
  scanf("%d", &a);
  while (scanf("%c", &c)) {
    switch (c) {
    case '+': {
      scanf("%d", &b);
      a += b;
      break;
    }
    case '-': {
      scanf("%d", &b);
      a -= b;
      break;
    }
    case '*': {
      scanf("%d", &b);
      a *= b;
      break;
    }
    case '/': {
      scanf("%d", &b);
      if (b == 0) {
        printf("ERROR\n");
        return 0;
      } else {
        a /= b;
        break;
      }
    }
    case '=': {
      printf("%d\n", a);
      return 0;
    }
    default: {
      printf("´íÎóµÄÔËËã·û£º%c", c);
      return 0;
    }
    }
  }
  return 0;
}

[Answer 2:]

#include <stdio.h>

main() {

  int c, flag = 1;
  int b;
  char ch;
  scanf("%d", &b);
  c = b;
  do {
    scanf("%c", &ch);
    if (ch == '=') {
      printf("%d", c);
      break;
    }
    scanf("%d", &b);
    if (ch == '+') {
      c += b;
    } else if (ch == '-') {
      c -= b;
    } else if (ch == '*') {
      c *= b;
    } else if (ch == '/') {
      c /= b;
    } else {
      printf("´íÎóµÄÔËËã·û£º%c", ch);
      flag = 0;
    }

  } while (flag == 1);
}

[Answer 3:]

#include <stdio.h>
int main() {
  int i, sum, isnan = 0, c;
  char op = '0';
  scanf("%d", &sum);
  while (op != '=') {
    scanf("%c", &op);
    if (op == '=')
      break;
    scanf("%d", &i);
    if (op == '+')
      sum = sum + i;
    else if (op == '-')
      sum = sum - i;
    else if (op == '*')
      sum = sum * i;
    else if (op == '/') {
      if (i != 0)
        sum = sum / i;
      else
        // printf("ERROR");
        isnan = 1;
    } else {
      isnan = 1;
      c = op;
    }
    // printf("ERROR");
  }
  if (isnan == 1)
    printf("´íÎóµÄÔËËã·û£º%c", c);
  else
    printf("%d\n", sum);
  return 0;
}

[Answer 4:]

#include <stdio.h>
int main() {
  char operator;
  int operand1, operand2, res;
  int flag = 0;
  scanf("%d", &operand1);
  operator= getchar();
  res = operand1;
  while (operator!= '=') {
    scanf("%d", &operand2);
    switch (operator) {
    case '+':
      res += operand2;
      break;
    case '-':
      res -= operand2;
      break;
    case '*':
      res *= operand2;
      break;
    case '/':
      res /= operand2;
      break;
    default:
      printf("´íÎóµÄÔËËã·û£º%c", operator);
      flag = 1;
      break;
    }
    operator= getchar();
  }
  if (flag == 0) {
    printf("%d\n", res);
  }
}


[4 answers found]