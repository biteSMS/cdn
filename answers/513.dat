[Answer 1:]

#include <stdio.h>
int max(int a, int b) { printf("max = %d\n", a > b ? a : b); }
int main() {
  int a, b;
  printf("Input a, b:");
  scanf("%d,%d", &a, &b);
  max(a, b);
  return 0;
}

[Answer 2:]

#include <stdio.h>
int max(int a, int b) { printf("max = %d\n", a > b ? a : b); }
int main() {
  int a, b;
  printf("Input a, b:");
  scanf("%d,%d", &a, &b);
  max(a, b);
  return 0;
}


[2 answers found]