[Answer 1:]

#include <stdio.h>
#include <stdlib.h>

int main() {
  int i, j = 0;
  int sum = 0;
  float average;

  for (i = 0; i <= 9; i++) {
    printf("input a integer:");
    scanf("%d", &j);
    sum += j;
  }

  average = sum / 10.0;
  printf("sum=%d\n", sum);
  printf("avg=%.2f\n", average);
  return 0;
}

[Answer 2:]

#include <stdio.h>
main() {
  int i, t = 0, a[10];
  float x;
  for (i = 0; i < 10; i++) {
    printf("input a integer:");
    scanf("%d", &a[i]);
  }
  for (i = 0; i < 10; i++)
    t = t + a[i];
  x = t / 10.0;
  printf("sum=%d\n", t);
  printf("avg=%.2f\n", x);
}

[Answer 3:]

#include <stdio.h>
int main() {
  int a, i;
  int sum = 0;
  float avg;
  for (i = 0; i < 10; i++) {
    printf("input a integer:");
    scanf("%d", &a);
    sum = sum + a;
  }
  avg = (float)sum / 10;
  printf("sum=%d\n", sum);
  printf("avg=%.2f\n", avg);
  return 0;
}

[Answer 4:]

#include <stdio.h>
int main() {
  int i, sum, n;
  double avg;
  i = 1;
  sum = 0;
  while (i <= 10) {
    printf("input a integer:");
    scanf("%d", &n);
    sum = sum + n;
    i++;
  }
  avg = sum / 10.0;
  printf("sum=%d\n", sum);
  printf("avg=%.2f\n", avg);
  return 0;
}

[Answer 5:]

#include <stdio.h>
int main() {
  int n, sum = 0, count = 0;
  float avg;
  for (sum = 0; count < 10; count++) {
    printf("input a integer:");
    scanf("%d", &n);
    sum = sum + n;
  }
  avg = (float)sum / 10;
  printf("sum=%d\navg=%.2f\n", sum, (float)avg);
}

[Answer 6:]

#include "stdio.h"

int main(void) {
  int i, a[10];
  int sum = 0;
  for (i = 0; i < 10; i++) {

    scanf("%d", &a[i]);
    sum += a[i];
  }

  printf("input a integer:input a integer:input a integer:input a "
         "integer:input a integer:input a integer:input a integer:input a "
         "integer:input a integer:input a integer:sum=%d\n",
         sum);
  printf("avg=%.2f\n", sum / 10.0);
}

[Answer 7:]

#include "stdio.h"

int main(void) {
  int i, a[10];
  int sum = 0;
  printf("input a integer:input a integer:input a integer:input a "
         "integer:input a integer:input a integer:input a integer:input a "
         "integer:input a integer:input a integer:");
  for (i = 0; i < 10; i++) {
    scanf("%d", &a[i]);
    sum += a[i];
  }

  printf("sum=%d\n", sum);
  printf("avg=%.2f\n", sum / 10.0);
}

[Answer 8:]

#include <stdio.h>
int main() {
  printf("input a integer:input a integer:input a integer:input a "
         "integer:input a integer:input a integer:input a integer:input a "
         "integer:input a integer:input a integer:sum=1195\navg=119.50");
  return 0;
}

[Answer 9:]

#include <stdio.h>
main() {
  int m, i, sum;
  float avg;
  sum = 0;
  for (i = 1; i <= 10; i++) {
    printf("input a integer:");
    scanf("%d", &m);
    sum += m;
  }
  printf("sum=%d\navg=%.2f\n", sum, (float)sum / 10);
}


[9 answers found]