[Answer 1:]

#include <stdio.h>
int main() {
  int x, y, z, count = 0;
  for (x = 1; x < 10; x++) {
    for (y = 1; y < 20; y++) {
      for (z = 1; z < 50; z++) {
        if (x * 10 + y * 5 + z == 100 && x + y + z == 50) {
          printf("x = %d, y = %d, z = %d\n", x, y, z);
          count++;
        }
      }
    }
  }
  printf("count = %d\n", count);
  return 0;
}

[Answer 2:]

#include <math.h>
#include <stdio.h>
#include <stdlib.h>
int main() {
  int count = 0, x, y, z;
  for (x = 1; x < 10; x++) {
    for (y = 1; y < 20; y++) {
      for (z = 1; z < 50; z++) {
        if (x * 10 + y * 5 + z == 100 && x + y + z == 50) {
          printf("x = %d, y = %d, z = %d\n", x, y, z);
          count++;
        }
      }
    }
  }
  printf("count = %d\n", count);
}

[Answer 3:]

#include <stdio.h>
main() {
  int x, y, z;
  int count = 0;
  for (x = 1; x <= 50; x++) {
    for (y = 1; y <= 50; y++) {
      for (z = 1; z <= 50; z++) {
        if (x * 10 + y * 5 + z == 100 && x + y + z == 50) {
          printf("x = %d, y = %d, z = %d\n", x, y, z);
          count++;
        }
      }
    }
  }
  printf("count = %d\n", count);
  return 0;
}

[Answer 4:]

#include <stdio.h>
main() {
  int a, b, c, i = 0;
  for (a = 0; a <= 10; a++) {
    for (b = 0; b <= 20; b++) {
      for (c = 0; c <= 100; c++) {
        if (a * 10 + b * 5 + c == 100 && a + b + c == 50) {
          i++;
          printf("x = %d, y = %d, z = %d\n", a, b, c);
        }
      }
    }
  }
  printf("count = %d\n", i);
}

[Answer 5:]

#include <stdio.h>
void main() {
  int x, y, z, count = 0;
  for (x = 1; x < 10; x++)
    for (y = 1; y < 20; y++)
      for (z = 1; z < 100; z++)
        if ((10 * x + 5 * y + z == 100) && (x + y + z == 50)) {
          printf("x = %d, y = %d, z = %d\n", x, y, z);
          count++;
          printf("count = %d\n", count);
        }
}

[Answer 6:]

#include <math.h>
#include <stdio.h>
#include <stdlib.h>

int main() {
  int a, b, c, count = 0;
  for (a = 0; a <= 10; a++) {
    for (b = 0; b <= 20; b++) {
      for (c = 0; c <= 100; c++) {
        if (a + b + c == 50 && a * 10 + b * 5 + c == 100) {
          ++count;
          printf("x = %d, y = %d, z = %d\n", a, b, c);
          printf("count = %d\n", count);
        }
      }
    }
  }
  return 0;
}

[Answer 7:]

#include <stdio.h>
#include <stdlib.h>
int main() {
  int x, y, z, i = 0;
  for (x = 1; x < 10; x++) {
    for (y = 1; y < 20; y++) {
      z = 50 - x - y;
      if (10 * x + 5 * y + z == 100) {
        printf("x = %d, y = %d, z = %d\n", x, y, z);
        i++;
      }
    }
  }
  printf("count = %d\n", i);
  return 0;
}

[Answer 8:]

#include <stdio.h>
int main() {
  int x, y, z, count = 0;
  for (x = 1; x < 10; x++)
    for (y = 1; y < 20; y++) {
      z = 50 - x - y;
      if (10 * x + 5 * y + z == 100) {
        printf("x = %d, y = %d, z = %d\n", x, y, z);
        count++;
      }
    }
  printf("count = %d\n", count);
  return 0;
}

[Answer 9:]

#include <stdio.h>
void main() {
  int x, y, z, count = 0;
  for (x = 1; x < 10; x++)
    for (y = 1; y < 20; y++)
      for (z = 1; z < 100; z++)
        if ((10 * x + 5 * y + z == 100) && (x + y + z == 50)) {
          printf("x = %d, y = %d, z = %d\n", x, y, z);
          count++;
          printf("count = %d\n", count);
        }
}

[Answer 10:]

#include <stdio.h>
void main() {
  int x, y, z, count = 0;
  for (x = 1; x < 10; x++)
    for (y = 1; y < 20; y++)
      for (z = 1; z < 100; z++)
        if ((10 * x + 5 * y + z == 100) && (x + y + z == 50)) {
          printf("x = %d, y = %d, z = %d\n", x, y, z);
          count++;
          printf("count = %d\n", count);
        }
}

[Answer 11:]

#include <stdio.h>
int main() {
  int x, y, z, count = 0;
  for (x = 0; x <= 5; x++) // 1
  {
    for (y = 0; y <= 10; y++) // 1
    {
      z = 50 - x - y;                // 1
      if (10 * x + 5 * y + z == 100) // 2
      {
        count++; // 1
        printf("x = %d, y = %d, z = %d\n", x, y, z);
      }
    }
  }
  printf("count = %d\n", count); // 1
  return 0;
}

[Answer 12:]

#include <stdio.h>
void main() {
  int x, y, z, sum = 0;
  for (x = 1; x < 50; x++) {
    for (y = 1; y < 50; y++) {
      if (10 * x + 5 * y + (50 - x - y) == 100) {
        printf("x = %d, y = %d, z = %d\n", x, y, 50 - x - y);
        sum++;
      }
    }
  }
  printf("count = %d\n", sum);
}

[Answer 13:]

#include <stdio.h>
int main() {
  int x, y, z, count = 0;
  for (x = 0; x <= 5; x++) // 1
  {
    for (y = 0; y <= 10; y++) // 1
    {
      z = 50 - x - y;                // 1
      if (10 * x + 5 * y + z == 100) // 2
      {
        count++; // 1
        printf("x = %d, y = %d, z = %d\n", x, y, z);
      }
    }
  }
  printf("count = %d\n", count); // 1
  return 0;
}

[Answer 14:]

#include <stdio.h>
main() {
  int x, y, z;
  int n = 0;
  for (x = 1; x <= 9; x++) {
    for (y = 1; y <= 17; y++) {
      10 * x + 5 * y + z == 100;
      z = 50 - x - y;
      if (10 * x + 5 * y + z == 100 && x + y + z == 50 && z > 0) {
        printf("x = %d, y = %d, z = %d\n", x, y, z);
        n++;
      }
    }
  }
  printf("count = %d\n", n);
}


[14 answers found]