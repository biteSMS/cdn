[Answer 1:]

#include <stdio.h>
int main() {
  int m = 0, d = 0, y = 0;
  printf("Enter a date(year month day):\n");
  scanf("%d%d%d", &y, &m, &d);
  printf("You entered the date: %02d/%02d/%d", m, d, y);
  return 0;
}

[Answer 2:]

#include <stdio.h>
int main() {
  int yy, mm, dd;
  printf("Enter a date(year month day):\n");
  scanf("%d%d%d", &yy, &mm, &dd);
  printf("You entered the date: %02d/%02d/%d", mm, dd, yy);
  return 0;
}

[Answer 3:]

#include <stdio.h>

int main() {
  int day, month, year;
  printf("Enter a date(year month day):\n");
  scanf("%d%d%d", &year, &month, &day);
  printf("You entered the date: %02d/%02d/%d", month, day, year);
}

[Answer 4:]

#include <stdio.h>
int main() {
  int a, b, c;
  printf("Enter a date(year month day):\n");
  scanf("%d%d%d", &a, &b, &c);
  printf("You entered the date: %02d/%02d/%d", b, c, a);
  return 0;
}

[Answer 5:]

#include <stdio.h>
main() {
  int a, b, c;
  printf("Enter a date(year month day):\n");
  scanf("%d%d%d", &a, &b, &c);
  printf("You entered the date: %02d/%02d/%d", b, c, a);
  return 0;
}

[Answer 6:]

#include <stdio.h>
main() {
  int a, b, c;
  printf("Enter a date(year month day):\n");
  scanf("%d%d%d", &a, &b, &c);
  printf("You entered the date: %02d/%02d/%d", b, c, a);
  return 0;
}

[Answer 7:]

#include <stdio.h>
#include <stdlib.h>

int main() {
  int y, m, d;

  printf("Enter a date(year month day):\n");
  scanf("%d%d%d", &y, &m, &d);

  printf("You entered the date: %02d/%02d/%d", m, d, y);
  return 0;
}

[Answer 8:]

#include <stdio.h>
int main() {
  int y, m, d;
  printf("Enter a date(year month day):\n");
  scanf("%d%d%d", &y, &m, &d);
  printf("You entered the date: %02d/%02d/%d", m, d, y);
  return 0;
}

[Answer 9:]

#include <stdio.h>
int main() {
  int y, m, d;
  printf("Enter a date(year month day):\n");
  scanf("%d%d%d", &y, &m, &d);
  printf("You entered the date: %02d/%02d/%d", m, d, y);
  return 0;
}

[Answer 10:]

#include <stdio.h>
int main() {
  int day, month, year;
  printf("Enter a date(year month day):\n");
  scanf("%d%d%d", &year, &month, &day);
  printf("You entered the date: %02d/%02d/%d", month, day, year);
  return 0;
}

[Answer 11:]

#include <stdio.h>
int main() {
  int a, b, c;
  printf("Enter a date(year month day):\n");
  scanf("%d%d%d", &a, &b, &c);
  printf("You entered the date: %02d/%02d/%d", b, c, a);
}

[Answer 12:]

#include <stdio.h>
int main() {
  int a, b, c;

  printf("Enter a date(year month day):\n");
  scanf("%d%d%d", &a, &b, &c);

  printf("You entered the date: %02d/%02d/%d", b, c, a);

  return 0;
}

[Answer 13:]

#include <stdio.h>
#include <stdlib.h>

#include <string.h>
int main() {
  printf("Enter a date(year month day):\n");
  int year, month, day;
  scanf("%d%d%d", &year, &month, &day);
  printf("You entered the date: %02d/%02d/%d", month, day, year);
  return 0;
}

[Answer 14:]

#include <stdio.h>
main() {
  int year, month, day;
  printf("Enter a date(year month day):\n");
  scanf("%d%d%d", &year, &month, &day);
  printf("You entered the date: %02d/%02d/%d", month, day, year);
}

[Answer 15:]

#include <stdio.h>
int main() {
  int y, m, d;
  printf("Enter a date(year month day):\n");
  scanf("%d%d%d", &y, &m, &d);
  printf("You entered the date: %02d/%02d/%d", m, d, y);
  return 0;
}

[Answer 16:]

#include <stdio.h>
main() {
  int a, b, c;
  printf("Enter a date(year month day):\n");
  scanf("%d%d%d", &a, &b, &c);
  printf("You entered the date: %02d/%02d/%d", b, c, a);
}

[Answer 17:]

#include <stdio.h>
main() {
  int a, b, c;
  printf("Enter a date(year month day):\n");
  scanf("%d%d%d", &a, &b, &c);
  printf("You entered the date: %02d/%02d/%d", b, c, a);
}

[Answer 18:]

#include <stdio.h>
#include <stdlib.h>

int main() {
  int y, m, d;
  printf("Enter a date(year month day):\n");
  scanf("%d%d%d", &y, &m, &d);
  printf("You entered the date: %02d/%02d/%d", m, d, y);
  return 0;
}

[Answer 19:]


#include <stdio.h>
#include <stdlib.h>

int main() {
  int y, m, d;
  printf("Enter a date(year month day):\n");
  scanf("%d%d%d", &y, &m, &d);
  printf("You entered the date: %02d/%02d/%d", m, d, y);
  return 0;
}

[Answer 20:]

#include <stdio.h>
main() {
  int a, b, c;
  printf("Enter a date(year month day):\n");
  scanf("%d%d%d", &a, &b, &c);
  printf("You entered the date: %02d/%02d/%d", b, c, a);
}

[Answer 21:]

#include <stdio.h>
int main() {
  int a, b, c;
  printf("Enter a date(year month day):\n");
  scanf("%d%d%d", &a, &b, &c);
  printf("You entered the date: %02d/%02d/%d", b, c, a);
  return 0;
}

[Answer 22:]

#include <stdio.h>
#include <stdlib.h>
int main() {
  int a, b, c, d, e;
  float f, g, h;
  char o, p, q;
  printf("Enter a date(year month day):\n");
  scanf("%d%d%d", &a, &b, &c);
  printf("You entered the date: %02d/%02d/%d", b, c, a);
  return 0;
}

[Answer 23:]

#include <stdio.h>
#include <stdlib.h>
int main() {
  int a, b, c;

  printf("Enter a date(year month day):\n");
  scanf("%d%d%d", &a, &b, &c);
  printf("You entered the date: %02d/%02d/%d", b, c, a);

  return 0;
}

[Answer 24:]

#include <math.h>
#include <stdio.h>
#include <stdlib.h>
int main() {
  int a, b, c, p, area;
  printf("Enter a date(year month day):\n");
  scanf("%d%d%d", &a, &b, &c);
  printf("You entered the date: %02d/%02d/%d", b, c, a);
  return 0;
}

[Answer 25:]

#include <stdio.h>
int main() {
  int a, b, c;
  printf("Enter a date(year month day):\n");
  scanf("%d%d%d", &a, &b, &c);
  printf("You entered the date: %02d/%02d/%d", b, c, a);
}

[Answer 26:]

#include <stdio.h>
main() {
  int year, month, day;
  printf("Enter a date(year month day):\n");
  scanf("%d %d%d", &year, &month, &day);
  printf("You entered the date: %02d/%02d/%d", month, day, year);
}

[Answer 27:]

#include <stdio.h>

main() {
  int y, m, d;
  printf("Enter a date(year month day):\n");
  scanf("%d%d%d", &y, &m, &d);
  printf("You entered the date: %02d/%02d/%d", m, d, y);
}

[Answer 28:]

#include <stdio.h>
int main() {
  printf("Enter a date(year month day):\n");
  int a, b, c;
  scanf("%d%d%d", &a, &b, &c);
  printf("You entered the date: %02d/%02d/%d", b, c, a);
}

[Answer 29:]

#include <math.h>
#include <stdio.h>
int main() {
  int y, m, d;

  printf("Enter a date(year month day):\n");
  scanf("%d%d%d", &y, &m, &d);
  printf("You entered the date: %02d/%02d/%d", m, d, y);
  return 0;
}

[Answer 30:]

#include <stdio.h>
#include <stdlib.h>

int main() {
  int a, b, c;
  printf("Enter a date(year month day):\n");
  scanf("%d %d %d", &a, &b, &c);
  printf("You entered the date: %02d/%02d/%d", b, c, a);
  return 0;
}

[Answer 31:]

#include <stdio.h>
#include <string.h>

void main() {
  int a, b, c;
  printf("Enter a date(year month day):\n");
  scanf("%d%d%d", &a, &b, &c);
  printf("You entered the date: %02d/%02d/%d", b, c, a);
}

[Answer 32:]

#include <stdio.h>
#include <stdlib.h>
int main() {
  int a, b, c;
  printf("Enter a date(year month day):\n");
  scanf("%d%d%d", &a, &b, &c);
  printf("You entered the date: %02d/%02d/%d", b, c, a);
}

[Answer 33:]

#include <math.h>
#include <stdio.h>
int main() {
  printf("Enter a date(year month day):\n");
  int a, b, c;
  scanf("%d%d%d", &a, &b, &c);

  printf("You entered the date: %02d/%02d/%d", b, c, a);
  return 0;
}


[33 answers found]