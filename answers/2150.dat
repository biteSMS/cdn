[Answer 1:]

#include <stdio.h>
#include <stdlib.h>

int main() {
  float x;
  printf("Please enter the number:");
  scanf("%f", &x);

  if (x < 0) {
    x = -x;
  }

  printf("%f", x);
  return 0;
}

[Answer 2:]

#include <stdio.h>
int main() {
  float a;
  printf("Please enter the number:");
  scanf("%f", &a);
  if (a > 0)
    printf("%f", a);
  else
    printf("%f", -a);
}

[Answer 3:]

#include <stdio.h>
int main() {
  float x;
  printf("Please enter the number:");
  scanf("%f", &x);
  if (x >= 0)
    printf("%f", x);
  else
    printf("%f", -x);
  return 0;
}

[Answer 4:]

#include <stdio.h>
main() {
  float x, y;
  printf("Please enter the number:");
  scanf("%f", &x);
  if (x >= 0)
    y = x;
  else {
    y = -x;
  }
  printf("%f", y);
  return 0;
}


[4 answers found]