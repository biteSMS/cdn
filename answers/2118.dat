[Answer 1:]

#include <stdio.h>
#include <stdlib.h>

int fun(int m);
int main() {
  int i, sum = 0;

  for (i = 100; i < 200; i++) {
    if (fun(i)) {
      sum += i;
    }
  }

  printf("sum=%d\n", sum);
  return 0;
}
int fun(int m) {
  int flag = 1;
  int i;

  for (i = 2; i < m; i++) {
    if (m % i == 0) {
      flag = 0;
      break;
    }
  }

  return flag;
}

[Answer 2:]

#include <stdio.h>
#include <stdlib.h>

int fun(int m);
int main() {
  int i;
  int sum = 0;
  for (i = 100; i < 200; i++) {
    if (fun(i)) {
      sum += i;
    }
  }

  printf("sum=%d\n", sum);

  return 0;
}
int fun(int m) {
  int flag = 1;
  int i;

  for (i = 2; i < m; i++) {
    if (m % i == 0) {
      flag = 0;
    }
  }

  return flag;
}

[Answer 3:]

#include <math.h>
#include <stdio.h>
int fun(int m) {
  int k, i;
  k = (int)(m / 2);
  for (i = 2; i < k; i++) {
    if (m % i == 0)
      return 0;
  }
  return 1;
}
int main() {
  int i, sum = 0;
  for (i = 100; i < 200; i++) {
    if (fun(i)) {
      sum = sum + i;
    }
  }
  printf("sum=%d\n", sum);
}


[3 answers found]