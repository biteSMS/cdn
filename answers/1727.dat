[Answer 1:]

#include <stdio.h>

void main() {
  long m, n, sum = 0;
  printf("Please enter n:");
  scanf("%ld", &n);
  for (m = 1; sum < n; m++) {
    sum += m * m * m;
  }
  printf("m<=%1d\n", m - 2);
}

[Answer 2:]

#include <stdio.h>
#include <stdlib.h>
int main() {
  long int n, sum = 0;
  int a = 1;
  printf("Please enter n:");
  scanf("%ld", &n);
  while (sum < n) {
    sum = sum + a * a * a;
    a++;
  }
  printf("m<=%1d\n", a - 2);
  return 0;
}

[Answer 3:]

#include <math.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
int main() {
  printf("Please enter n:");

  long n, sum = 0;
  int i = 1;
  scanf("%ld", &n);

  do {
    sum += i * i * i;
    i++;
  } while (sum <= n);
  printf("m<=%1d\n", i - 2);
  return 0;
}

[Answer 4:]

#include <stdio.h>
#include <stdlib.h>

int main() {
  int m = 1;
  long int sum = 0, n;
  printf("Please enter n:");
  scanf("%ld", &n);
  while (sum < n) {
    sum += pow(m, 3);
    m++;
  }
  printf("m<=%1d\n", m - 2);
}

[Answer 5:]

#include <stdio.h>
int main() {
  printf("Please enter n:");
  int n, sum, m;
  scanf("%ld", &n);
  sum = 0;
  m = 1;
  while (sum < n) {
    sum += m * m * m;
    m++;
  }
  printf("m<=%1d\n", m - 2);
}

[Answer 6:]

#include <stdio.h>
int main() {
  long m;
  int n, sum = 0, i;
  printf("Please enter n:");
  scanf("%ld", &n);
  for (i = 0; sum < n; ++i) {
    sum += i * i * i;
    m = i;
  }
  printf("m<=%1d\n", m - 1);
  return 0;
}

[Answer 7:]

#include <stdio.h>
#include <stdlib.h>

int main() {
  int n = 0, i = 1, x, y, z, sum = 0;
  printf("Please enter n:");
  scanf("%d", &n);
  for (i = 1; i <= 100; i++) {
    sum = sum + i * i * i;
    if (sum < n) {
      x = i;
    }
  }
  printf("m<=%1d\n", x);
  return 0;
}

[Answer 8:]

#include <math.h>
#include <stdio.h>
int main() {
  long n, sum;
  int i;
  printf("Please enter n:");
  scanf("%ld", &n);
  for (i = 1; sum < n; i++) {
    sum += pow(i, 3);
  }
  printf("m<=%1d\n", i - 2);
  return 0;
}

[Answer 9:]

#include "math.h"
#include "stdio.h"
int main() {
  int m;
  long n;
  long i;
  printf("Please enter n:");
  scanf("%ld", &n);
  m = 2 * ((int)sqrt(n) + 1);
  for (i = m; i >= 0; i--)
    if (pow(i, 2) * pow(1 + i, 2) < 4 * n) {
      printf("m<=%1d\n", i);
      break;
    }
  return 0;
}

[Answer 10:]

#include <stdio.h>
int main() {
  printf("Please enter n:");
  long int n;
  scanf("%ld", &n);
  int i, sum = 0;
  for (i = 0; sum < n; i++) {
    sum += i * i * i;
  }

  sum = sum - i * i * i;

  printf("m<=%1d\n", i - 2);
}

[Answer 11:]

#include <math.h>
#include <stdio.h>
#include <stdlib.h>

int main() {
  long i = 0, n, result = 0;
  printf("Please enter n:");
  scanf("%ld", &n);

  do {
    i++;
    result += pow(i, 3);
  } while (result < n);

  printf("m<=%1d\n", i - 1);

  return 0;
}

[Answer 12:]

#include <math.h>
#include <stdio.h>

int main() {
  printf("Please enter n:");
  long n, sum = 0;
  int i;
  scanf("%ld", &n);
  for (i = 1; sum < n; i++) {
    sum = sum + pow(i, 3);
  }
  printf("m<=%1d\n", i - 2);
  return 0;
}

[Answer 13:]

#include <stdio.h>
#include <stdlib.h>
int main() {
  long n, m, a = 0, i;
  printf("Please enter n:");
  scanf("%ld", &n);
  for (i = 1;; i++) {
    a = a + i * i * i;
    if (a >= n) {
      i--;
      printf("m<=%1d\n", i);
      break;
    }
  }
  return 0;
}

[Answer 14:]

#include <math.h>
#include <stdio.h>
int main() {
  int a, n, m, j = 0;
  printf("Please enter n:");
  scanf("%ld", &n);
  for (m = 1; pow(m, 3) < n; m++) {
    j += pow(m, 3);
    if (j < n)
      a = m;
  }
  printf("m<=%1d\n", a);
}

[Answer 15:]

#include <stdio.h>
int main() {
  int n, m = 0, i;
  printf("Please enter n:");
  scanf("%ld", &n);
  for (i = 1; m < n;) {
    m += i * i * i;
    i++;
  }
  printf("m<=%1d\n", i - 2);
}

[Answer 16:]

#include <stdio.h>
int main() {
  long j, i, n, m = 0;
  printf("Please enter n:");
  scanf("%d", &n);
  for (i = 1, j = 1; i > 0; i++, j++) {
    m = i * i * i + m;
    if (m >= n)
      i = -1;
  }
  printf("m<=%1d\n", j - 2);
  return 0;
}

[Answer 17:]

#include <stdio.h>
main() {
  int m;
  long n, sum = 0;
  printf("Please enter n:");
  scanf("%ld", &n);
  for (m = 0; sum < n; m++) {
    sum = sum + m * m * m;
  }
  printf("m<=%1d\n", m - 2);
}

[Answer 18:]

#include <math.h>
#include <stdio.h>
main() {
  long n, sum, i = 0;
  printf("Please enter n:");
  scanf("%ld", &n);
  for (i = 0; sum < n; i++) {
    sum += pow(i, 3);
  }
  printf("m<=%ld\n", i - 2);
}

[Answer 19:]

#include <stdio.h>
int main() {
  int m, n, i = 0;
  printf("Please enter n:");
  scanf("%d", &n);
  for (m = 1; m <= n; m++) {
    i = i + m * m * m;
    if (i >= n) {
      printf("m<=%1d\n", m - 1);
      break;
    }
  }
}

[Answer 20:]

#include <stdio.h>
main() {
  long n;
  printf("Please enter n:");
  scanf("%ld", &n);
  int i, j;
  long sum = 0;
  for (i = 1; sum < n; i++) {
    j = i * i * i;
    sum += j;
  }
  i = i - 2;
  printf("m<=%1d\n", i);
}


[20 answers found]