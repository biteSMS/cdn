[Answer 1:]

#include <stdio.h>
int main() {
  int x, y;
  for (x = 0; x < 99; x++) {
    y = 98 - x;
    if (2 * x + 4 * y == 386) {
      printf("x = %d,y = %d\n", x, y);
      break;
    }
  }
}

[Answer 2:]

#include <stdio.h>
main() {
  int i, j;
  for (i = 0; i < 98; i++) {
    j = 98 - i;
    if (i * 2 + j * 4 == 386) {
      printf("x = %d,y = %d\n", i, j);
    }
  }
}

[Answer 3:]

#include <math.h>
#include <stdio.h>
int main() {
  int a, b;
  for (a = 1; a < 2000; a++) {
    for (b = 1; b < 2000; b++) {
      if ((a + b == 98) && 4 * b + 2 * a == 386)
        printf("x = %d,y = %d\n", a, b);
    }
  }
  return 0;
}

[Answer 4:]

#include <stdio.h>
int main() {
  int m, n;
  printf("x = %d,y = %d\n", 3, 95);
}

[Answer 5:]

#include <stdio.h>
main() {
  int chick, rabit;
  for (chick = 0; chick <= 98; chick++) {
    rabit = 98 - chick;
    if (chick * 2 + rabit * 4 == 386)
      printf("x = %d,y = %d\n", chick, rabit);
  }
}

[Answer 6:]

#include <stdio.h>
main() {
  int x, y;
  for (x = 1; x <= 98; x++) // 4
  {
    for (y = 1; y < 98; y++) // 4
    {
      if (x + y == 98 && 2 * x + 4 * y == 386) // 4
      {
        printf("x = %d,y = %d\n", x, y); // 2
      }
    }
  }
}

[Answer 7:]

#include <stdio.h>
int main() {
  int t, j;
  for (t = 0; t < 98; t++)
    for (j = 0; j < 98; j++) {
      if (t + j == 98 && 4 * t + 2 * j == 386)
        printf("x = %d,y = %d\n", j, t);
    }
  return 0;
}

[Answer 8:]

#include <stdio.h>
#include <stdlib.h>

main() {
  int i;
  for (i = 1; i <= 98; i++) {
    if (2 * i + 4 * (98 - i) == 386)
      break;
  }
  printf("x = %d,y = %d\n", i, (98 - i));
}

[Answer 9:]

#include <stdio.h>
int main() {
  int m, n;
  for (m = 1; m <= 98; m++) {
    if (2 * m + 4 * (98 - m) == 386) {
      n = 98 - m;
      break;
    }
  }
  printf("x = %d,y = %d\n", m, n);
}

[Answer 10:]

#include <stdio.h>
#include <stdlib.h>

int main() {
  int x, y;
  for (x = 0; x < 98; x++) {
    for (y = 0; y < 98; y++) {
      if (x + y == 98 && x * 2 + y * 4 == 386)
        printf("x = %d,y = %d\n", x, y);
    }
  }
  return 0;
}


[10 answers found]