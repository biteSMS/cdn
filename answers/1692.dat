[Answer 1:]

#include <stdio.h>

int main() {
  int year;
  printf("Input a year:");
  scanf("%d", &year);
  if ((year % 4 == 0 && year % 100 != 0) || year % 400 == 0) {
    printf("%d is a leap year!\n", year);
  } else {
    printf("%d is not a leap year!\n", year);
  }
  return 0;
}

[Answer 2:]

#include <stdio.h>
main() {
  int year;
  printf("Input a year:");
  scanf("%d", &year);
  if ((year % 4 == 0 && year % 100 != 0) || year % 400 == 0) {
    printf("%d is a leap year!\n", year);
  } else {
    printf("%d is not a leap year!\n", year);
  }
}

[Answer 3:]

#include <stdio.h>
main() {
  int year;
  printf("Input a year:");
  scanf("%d", &year);
  if ((year % 4 == 0 && year % 100 != 0) || year % 400 == 0) {
    printf("%d is a leap year!\n", year);
  } else {
    printf("%d is not a leap year!\n", year);
  }
}

[Answer 4:]

#include <stdio.h>
int main() {
  int n;
  printf("Input a year:");
  scanf("%d", &n);
  if ((n % 4 == 0 && n % 100 != 0) || (n % 400 == 0)) {
    printf("%d is a leap year!\n", n);
  } else {
    printf("%d is not a leap year!\n", n);
  }
  return 0;
}

[Answer 5:]

#include <stdio.h>
#include <stdlib.h>
int main() {
  int x;
  printf("Input a year:");
  scanf("%d", &x);
  if (x % 400 == 0 || x % 4 == 0 && x % 100 != 0) {
    printf("%d is a leap year!\n", x);
  } else {
    printf("%d is not a leap year!\n", x);
  }
}

[Answer 6:]

#include <stdio.h>
#include <stdlib.h>
int main() {
  int year;
  printf("Input a year:");
  scanf("%d", &year);
  if ((year % 4 == 0 && year % 100 != 0) || year % 400 == 0)
    printf("%d is a leap year!\n", year);
  else
    printf("%d is not a leap year!\n", year);
  return 0;
}

[Answer 7:]

#include <stdio.h>
#include <stdlib.h>

#include <string.h>
int main() {
  printf("Input a year:");
  int n;
  scanf("%d", &n);
  if ((n % 4 == 0) && (n % 10 != 0) || (n % 400 == 0)) {
    printf("%d is a leap year!\n", n);
  } else {
    printf("%d is not a leap year!\n", n);
  }
  return 0;
}

[Answer 8:]

#include <math.h>
#include <stdio.h>
#include <stdlib.h>
int main() {
  int y;
  printf("Input a year:");
  scanf("%d", &y);
  if (y % 4 == 0 && y % 100 != 0 || y % 400 == 0) {
    printf("%d is a leap year!\n", y);
  } else {
    printf("%d is not a leap year!\n", y);
  }
  return 0;
}

[Answer 9:]

#include <stdio.h>
#include <stdlib.h>
int main() {
  int h;
  float a, b, c, d, x, y;
  char e, f, g;
  printf("Input a year:");
  scanf("%d", &h);
  if (h % 4 == 0 && h % 100 != 0 || h % 400 == 0) {
    printf("%d is a leap year!\n", h);
  } else {
    printf("%d is not a leap year!\n", h);
  }
  return 0;
}

[Answer 10:]

#include <stdio.h>

int main() {
  int x;

  printf("Input a year:");
  scanf("%d", &x);
  if (x % 4 == 0 && x % 100 != 0 || x % 400 == 0)
    printf("%d is a leap year!\n", x);
  else
    printf("%d is not a leap year!\n", x);

  return 0;
}

[Answer 11:]

#include <stdio.h>
#include <stdlib.h>

int main() {
  int n;
  printf("Input a year:");
  scanf("%d", &n);
  if ((n % 4 == 0 && n % 100 != 0) || n % 400 == 0)
    printf("%d is a leap year!\n", n);
  else
    printf("%d is not a leap year!\n", n);
  return 0;
}

[Answer 12:]

#include <math.h>
#include <stdio.h>
void main(void) {
  int year, a;
  printf("Input a year:");
  scanf("%d", &year);
  if ((year % 4 == 0 && year % 100 != 0) || (year % 400 == 0))
    a = 1;
  else
    a = 0;
  if (a == 1)
    printf("%d is a leap year!\n", year);
  else
    printf("%d is not a leap year!\n", year);
}

[Answer 13:]

#include <stdio.h>
int main() {
  int y;
  printf("Input a year:");
  scanf("%d", &y);
  if (y % 4 == 0 && y % 100 != 0 || y % 400 == 0) {
    printf("%d is a leap year!\n", y);
  } else
    printf("%d is not a leap year!\n", y);
}

[Answer 14:]

#include <stdio.h>
int main() {
  printf("Input a year:");
  int n;
  scanf("%d", &n);
  if (n % 4 == 0 && n % 100 != 0 || n % 400 == 0) {
    printf("%d is a leap year!\n", n);
    return 0;
  }

  printf("%d is not a leap year!\n", n);
  return 0;
}

[Answer 15:]

#include <stdio.h>
int main() {
  int year;
  printf("Input a year:");
  scanf("%d", &year);
  if (((year % 4 == 0) && (year % 100 != 0)) || (year % 400 == 0)) {
    printf("%d is a leap year!\n", year);
  } else {
    printf("%d is not a leap year!\n", year);
  }
  return 0;
}

[Answer 16:]

//从键盘任意输入一个年号，判断它是否是闰年。已知符合下列条件之一者是闰年：（1）能被4整除，但不能被100整除；（2）能被400整除。
//输入提示信息："Input a year:"
//输入格式："%d"
//输出提示信息和格式：
//"%d is a leap year!\n"
//"%d is not a leap year!\n"

#include <stdio.h>

int is_runnian(int x) {
  if ((x % 4 == 0) && (x % 100 != 0) || (x % 400 == 0))
    return 1;
  else
    return 0;
}

int main() {
  printf("Input a year:");
  int x = 0;
  scanf("%d", &x);
  printf("%d%s\n", x,
         is_runnian(x) ? " is a leap year!" : " is not a leap year!");
  return 0;
}

[Answer 17:]

#include <ctype.h>
#include <stdio.h>
#include <string.h>
int main() {
  int m;
  printf("Input a year:");
  scanf("%d", &m);
  if ((m % 4 == 0 && m % 100 != 0) || m % 400 == 0)
    printf("%d is a leap year!\n", m);
  else
    printf("%d is not a leap year!\n", m);
  return 0;
}

[Answer 18:]

#include <math.h>
#include <stdio.h>
#include <string.h>

int main() {
  int a;
  printf("Input a year:");
  scanf("%d", &a);
  if ((a % 4 == 0 && a % 100 != 0) || (a % 400 == 0))
    printf("%d is a leap year!\n", a);
  else
    printf("%d is not a leap year!\n", a);
}

[Answer 19:]

#include <stdio.h>
int main() {
  int year;
  printf("Input a year:", year);
  scanf("%d", &year);
  if (year % 4 == 0 && year % 100 != 0 || year % 400 == 0)
    printf("%d is a leap year!\n", year);
  else
    printf("%d is not a leap year!\n", year);
}

[Answer 20:]

#include <stdio.h>
int main() {
  int a;
  printf("Input a year:");
  scanf("%d", &a);
  if (a % 4 == 0 && a % 100 != 0 || a % 400 == 0)
    printf("%d is a leap year!\n", a);
  else
    printf("%d is not a leap year!\n", a);
}

[Answer 21:]

#include <stdio.h>
int main() {
  int year;
  printf("Input a year:");
  scanf("%d", &year);
  if (year % 4 == 0 && year % 100 != 0 || year % 400 == 0)
    printf("%d is a leap year!\n", year);
  else
    printf("%d is not a leap year!\n", year);
  return 0;
}

[Answer 22:]

#include <stdio.h>
int main() {
  int x;
  printf("Input a year:");
  scanf("%d", &x);
  if (x % 400 == 0 || x % 4 == 0 && x % 100 != 0)
    printf("%d is a leap year!\n", x);
  else
    printf("%d is not a leap year!\n", x);
  return 0;
}

[Answer 23:]

#include <stdio.h>
int main() {
  int year, flag;
  printf("Input a year:");
  scanf("%d", &year);
  if (year % 4 == 0 && year % 100 != 0)
    flag = 1;
  else
    flag = 0;
  if (flag == 1)
    printf("%d is a leap year!\n", year);
  else
    printf("%d is not a leap year!\n", year);
  return 0;
}

[Answer 24:]

#include <stdio.h>
int main() {
  int year;
  printf("Input a year:");
  scanf("%d", &year);
  if (year % 4 == 0 && year % 400 != 0 || year % 400 == 0)
    printf("%d is a leap year!\n", year);
  else
    printf("%d is not a leap year!\n", year);
  return 0;
}

[Answer 25:]

#include <stdio.h>
main() {
  int n;
  printf("Input a year:");
  scanf("%d", &n);
  if ((n % 4 == 0 && n % 100 != 0) || n % 400 == 0)
    printf("%d is a leap year!\n", n);
  else
    printf("%d is not a leap year!\n", n);
}

[Answer 26:]

#include <stdio.h>
int main() {
  int x;
  printf("Input a year:");
  scanf("%d", &x);
  if ((x % 4 == 0 && x % 100 != 0) || x % 400 == 0)
    printf("%d is a leap year!\n", x);
  else
    printf("%d is not a leap year!\n", x);
  return 0;
}


[26 answers found]