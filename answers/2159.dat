[Answer 1:]

#include <stdio.h>

#define persons 45;
#define tricks 45;

int main() {
  int men, women, kids;
  for (men = 0; men < 16; men++)
    for (women = 0; women < 23; women++)
      for (kids = 0; kids < 91; kids++) {
        if (men + women + kids == 45 && men * 3 + women * 2 + kids / 2.0 == 45)
          printf("men=%d,women=%d,child=%d\n", men, women, kids);
      }
}

[Answer 2:]

#include <stdio.h>
#include <stdlib.h>
int main() {
  int a, b, c;
  for (a = 0; a <= 15; a++) {
    for (b = 0; b <= 23; b++) {
      for (c = 0; c <= 90; c = c + 2) {
        if (a * 3 + b * 2 + c / 2 == 45 && a + b + c == 45) {
          printf("men=%d,women=%d,child=%d\n", a, b, c);
        }
      }
    }
  }
}

[Answer 3:]

#include <stdio.h>
int main() {
  int a, b, c, n;
  for (a = 0; a <= 15; a++) {
    for (b = 0; b <= 22; b++) {
      for (c = 0; c <= 45; c += 2) {
        if (a + b + c == 45 && a * 3 + b * 2 + c / 2 == 45)
          printf("men=%d,women=%d,child=%d\n", a, b, c);
      }
    }
  }
  return 0;
}

[Answer 4:]

#include <stdio.h>

int main() {
  int a, b, c;
  for (a = 0; a <= 45; a++) {
    for (c = 0; c <= (45 - a * 3) * 2; c += 2) {
      b = 45 - a - c;
      if (a * 3 + b * 2 + c / 2 == 45)
        printf("men=%d,women=%d,child=%d\n", a, b, c);
    }
  }
  return 0;
}

[Answer 5:]

#include <stdio.h>
int main() {
  int a, b, c, n;
  for (a = 0; a <= 15; a++) {
    for (b = 0; b <= 22; b++) {
      for (c = 0; c <= 45; c += 2) {
        if (a + b + c == 45 && a * 3 + b * 2 + c / 2 == 45)
          printf("men=%d,women=%d,child=%d\n", a, b, c);
      }
    }
  }
  return 0;
}

[Answer 6:]

#include <stdio.h>
main() {
  int i, j, k;
  for (i = 0; i < 15; i++)
    for (j = 0; j < 25; j++)
      for (k = 0; k < 45; k++) {
        if (i + k + j == 45 && 3 * i + 2 * j + 1. * k / 2 == 45)
          printf("men=%d,women=%d,child=%d\n", i, j, k);
      }
}

[Answer 7:]

#include <stdio.h>
main() {
  int i, j, k;
  for (i = 0; i < 15; i++)
    for (j = 0; j < 25; j++)
      for (k = 0; k < 45; k++) {
        if (i * 3 + j * 2 + k * 1.0 / 2 == 45 && i + k + j == 45)
          printf("men=%d,women=%d,child=%d\n", i, j, k);
      }
}

[Answer 8:]

#include <stdio.h>
int main() {
  int i, j, k;
  for (i = 0; i < 15; ++i) {
    for (j = 0; j < 25; ++j) {
      for (k = 0; 2 * k < 45; ++k) {
        if (i + j + 2 * k == 45 && 3 * i + 2 * j + k == 45) {
          printf("men=%d,women=%d,child=%d\n", i, j, 2 * k);
        }
      }
    }
  }
  return 0;
}

[Answer 9:]

#include <stdio.h>
int main() {
  int i, j, k;
  for (i = 0; i < 15; ++i) {
    for (j = 0; j < 28; ++j) {
      for (k = 0; k < 45; k++) {
        if (i + j + 2 * k == 45 && 3 * i + 2 * j + k == 45)
          printf("men=%d,women=%d,child=%d\n", i, j, 2 * k);
      }
    }
  }
  return 0;
}

[Answer 10:]

#include <stdio.h>
#include <stdlib.h>

int main() {
  int n = 0, i = 1, x = 1, y = 1, z, sum = 0;
  float a;
  for (x = 0; x <= 45; x++) {
    for (y = 0; y <= 45; y++) {
      z = 45 - x - y;
      if (3 * x + 2 * y + z / 2.0 == 45) {
        printf("men=%d,women=%d,child=%d\n", x, y, z);
      }
    }
  }
  return 0;
}

[Answer 11:]

#include <stdio.h>
main() {
  int man, wm, cl, a, b, c;
  for (man = 0; man <= 15; man++) {
    for (wm = 0; wm <= 22; wm++) {
      a = man * 3;
      b = wm * 2;
      cl = 45 - man - wm;
      c = cl / 2;
      if (a + b + c == 45 && man + wm + cl == 45 && cl % 2 == 0)
        printf("men=%d,women=%d,child=%d\n", man, wm, cl);
    }
  }
}

[Answer 12:]

#include <stdio.h>
int main() {
  int x, y, z;

  for (x = 0; x <= 15; x++)
    for (y = 0; y <= 22; y++)
      for (z = 0; z <= 90; z++)
        if (3 * x + 2 * y + z / 2 == 45 && z % 2 == 0 && x + y + z == 45)
          printf("men=%d,women=%d,child=%d\n", x, y, z);
}

[Answer 13:]

#include <stdio.h>
int main() {
  int a, b, c;
  for (a = 0; a < 15; a++) {
    for (b = 0; b < 23; b++) {
      for (c = 0; c < 45; c++) {
        if (a + b + c == 45 && c % 2 == 0 && a * 3 + b * 2 + c / 2 == 45) {
          printf("men=%d,women=%d,child=%d\n", a, b, c);
        }
      }
    }
  }

  return 0;
}


[13 answers found]