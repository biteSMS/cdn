[Answer 1:]

#include <stdio.h>
#include <string.h>
int Convert(char b[], char a[]);
int main() {
  char str[80], str0[] = {"is"};
  int count = 0;
  gets(str);
  count = Convert(str, str0);
  if (count == 0) {
    printf("not found!\n");
  } else {

    printf("%d found!\n%s\n", count, str);
  }
}
int Convert(char b[], char a[]) {
  int i, count = 0;

  for (i = 0; b[i + 1] != '\0'; i++) {

    if (b[i] == a[0] && b[i + 1] == a[1]) {
      b[i] = 'b';
      b[i + 1] = 'e';
      count++;
    }
  }
  return count;
}

[Answer 2:]

#include "stdio.h"
int Convert(char b[], char a[]);
main() {
  char a[20], b[20];
  gets(a);
  Convert(b, a);
}
int Convert(char b[], char a[]) {
  int i = 0, order = 0;
  while (a[i + 1]) {
    if (a[i] == 'i' && a[i + 1] == 's') {
      a[i] = 'b';
      a[i + 1] = 'e';
      order++;
    }
    i++;
  }
  if (order) {
    printf("%d found!\n%s\n", order, a);
  } else {
    printf("not found!\n");
  }
}


[2 answers found]