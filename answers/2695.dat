[Answer 1:]

#include <stdio.h>

int Gcd(int a, int b);

int main() {
  int a[10], i, j, k;
  printf("Input 10 numbers:");
  for (i = 0; i < 10; i++) {
    scanf("%d", &a[i]);
  }
  for (i = 0; i < 9; i++) {
    for (j = 1; j < 10; j++) {
      if (a[j] < a[j - 1]) {
        k = a[j];
        a[j] = a[j - 1];
        a[j - 1] = k;
      }
    }
  }
  printf("%d", Gcd(a[0], a[9]));

  return 0;
}

int Gcd(int a, int b) {
  int gcd;
  while (b % a != 0) {
    gcd = b % a;
    b = a;
    a = gcd;
  }
  return a;
}

[Answer 2:]

#include "stdio.h"
int gcd(int i, int j) {
  if (i < 1 || j < 1)
    return -1;
  if (i % j == 0)
    return j;
  else
    return gcd(j, i % j);
}
int main() {
  int a[11], i, max, min, ans;
  printf("Input 10 numbers:");
  for (i = 1; i <= 10; i++)
    scanf("%d", &a[i]);
  max = a[1];
  min = a[1];
  for (i = 2; i <= 10; i++) {
    if (a[i] > max)
      max = a[i];
    if (a[i] < min)
      min = a[i];
  }
  ans = gcd(max, min);
  if (ans != -1)
    printf("%d", ans);
  return 0;
}

[Answer 3:]

#include <stdio.h>
main() {
  int i, a[10], min, max, yue;
  printf("Input 10 numbers:");

  for (i = 0; i < 10; i++) {
    scanf("%d", a + i);
  }

  min = a[0];
  max = a[0];
  for (i = 0; i < 10; i++) {
    if (a[i] >= max) {
      max = a[i];
    } else {
      min = a[i];
    }
  }

  for (i = 1; i <= min; i++) {
    if (min % i == 0 && max % i == 0) {
      yue = i;
    }
  }
  printf("%d", yue);
}

[Answer 4:]

#include "stdio.h"

main() {
  int num[10] = {0}, i = 0, max, min;
  printf("Input 10 numbers:");
  for (; i < 10; i++) {
    scanf("%d", &num[i]);
  }
  max = num[0];
  min = num[0];
  for (i = 0; i < 10; i++) {
    if (max < num[i]) {
      max = num[i];
    }
    if (min > num[i]) {
      min = num[i];
    }
  }
  for (i = min;; i--) {
    if (min % i == 0 && max % i == 0) {
      printf("%d", i);
      break;
    }
  }
}


[4 answers found]