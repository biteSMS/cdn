[Answer 1:]

#include <stdio.h>
int main() {
  int i;
  float sum = 0.0;
  for (i = 2; i <= 100; i = i + 2) {
    sum += (float)(1.0 / ((i - 1) * i * (i + 1)));
  }
  printf("sum=%f\n", sum);
}

[Answer 2:]

#include <math.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
int main() {
  int i;
  float sum = 0;
  for (i = 1; i < 99; i = i + 2) {
    sum += 1 / (i * (i + 1) * (i + 2) * 1.0);
  }
  printf("sum=%f\n", sum);
  return 0;
}

[Answer 3:]

#include <stdio.h>
#include <stdlib.h>

int main() {
  int a, b, c;
  float sum = 0;
  for (a = 2; a <= 100; a += 2)
    sum += 1.0 / ((a - 1) * a * (a + 1));
  printf("sum=%f\n", sum);
}


[3 answers found]